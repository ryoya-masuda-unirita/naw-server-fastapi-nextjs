# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `schemas/feedback.py` に `FeedbackUserListResponse` を追加、3つの `Paged*Response` に `totalPages`・`numberOfElements` を追加
- [x] `routers/feedback.py` の `get_feedback_users` を `FeedbackUserListResponse` へ変更
- [x] `services/feedback_service.py` の3メソッドで `totalPages`・`numberOfElements` を計算・設定

## テスト

- [x] `test_feedback.py` の feedbackUser 関連テストを `feedbacks` ラッパー前提に更新
- [x] `test_feedback.py` の3つのページングテストに `totalPages`・`numberOfElements` の検証を追加
- [x] `uv run pytest` で全テスト成功を確認（761 passed）
- [x] `uv run mypy app` で型エラーがないことを確認
- [x] `uv run ruff check app` でlintエラーがないことを確認

## 動作確認

- [x] `feedbackUser`・`feedbackMessage`・`feedbackRoom` の3APIを実際に呼び、レスポンス構造とページング値を確認
- [x] frontend-angular経由で「回答数一覧」タブがクラッシュせず表示されることを確認
- [x] 動作確認を `08_動作確認.md` に記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
