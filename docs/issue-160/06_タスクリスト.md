# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 準備

- [x] `infra/`ディレクトリ・`.gitignore`（`*.tfstate*`・`*.tfvars`・`.terraform/`）を作成
- [x] Terraformをローカルにインストール（v1.15.8）

## 実装（ファイル単位、ハイブリッド形式で進める）

- [x] `main.tf`（providerブロック、us-east-1エイリアス）・`variables.tf`・`terraform.tfvars.example`（`terraform validate`成功を確認）
- [x] `network.tf`（VPC・サブネット2つ・IGW・ルートテーブル、`for_each`利用、`plan`で7 to add確認済み）
- [x] `security_groups.tf`（SG5つ、`terraform validate`成功を確認）
- [x] `ecr.tf`（`terraform validate`成功を確認）
- [x] `rds.tf`・`elasticache.tf`（`terraform validate`成功を確認）
- [x] `bastion.tf`（`terraform validate`成功を確認）
- [ ] `ecs.tf`（クラスタ・ASG・タスク定義・サービス・IAMロール）
- [ ] `alb.tf`
- [ ] `route53.tf`（ホストゾーン作成、ムームードメイン側NS変更は手動）
- [ ] `acm.tf`（DNS検証をRoute53で自動化）
- [ ] `s3_cloudfront.tf`
- [ ] `outputs.tf`

## テスト・動作確認

- [ ] `terraform init`/`validate`（No.1）
- [ ] `terraform plan`初回実行、想定リソース数と一致することを確認（No.2）
- [ ] `terraform apply`実行（No.3）
- [ ] ACM証明書のDNS検証をムームーDNSに登録し発行を確認（No.8）
- [ ] Issue #158相当の動作確認（backend起動・RDS疎通・ALB疎通・CloudFront経由ログイン・S3遮断等）（No.4）
- [ ] `terraform plan`再実行し差分ゼロを確認（No.5）
- [ ] `terraform destroy`実行（No.6）
- [ ] 削除後の残存確認（No.7）
- [ ] `08_動作確認.md`に結果を記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
