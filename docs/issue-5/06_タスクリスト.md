# 06_タスクリスト

## タスク一覧

### DB 環境
- [ ] `docker-compose.yml` をプロジェクトルートに作成する
- [ ] `docker compose up -d` で PostgreSQL が起動することを確認する

### 依存パッケージ
- [ ] `backend/pyproject.toml` に `alembic>=1.13.0` を追加する
- [ ] `uv sync` で alembic がインストールされることを確認する

### Alembic セットアップ
- [ ] `backend/alembic.ini` を作成する
- [ ] `backend/alembic/env.py` を作成する（非同期対応）
- [ ] `backend/alembic/script.py.mako` を作成する

### モデル定義
- [ ] `backend/app/models/tenant.py` を作成する
- [ ] `backend/app/models/user.py` を作成する

### マイグレーション
- [ ] `backend/alembic/versions/001_init_tenants_users.py` を作成する
- [ ] `alembic upgrade head` でテーブルが作成されることを確認する

### DB 接続設定
- [ ] `backend/app/core/database.py` を作成する

### テスト
- [ ] `backend/tests/conftest.py` を作成する
- [ ] `backend/tests/test_models.py` を作成する
- [ ] `pytest` を実行して全テストがパスすることを確認する

## 完了条件

- `docker compose up -d` で PostgreSQL が起動すること
- `alembic upgrade head` が正常終了し `tenants`・`users` テーブルが作成されること
- `pytest` で全テストケースがパスすること
