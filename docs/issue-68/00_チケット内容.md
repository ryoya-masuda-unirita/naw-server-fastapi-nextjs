# AuthController ログインキー認証APIの移植

## 概要

`~/Documents/naw-server`（Spring Boot）の `AuthController#authenticateWithLoginKey` をFastAPI（`backend/`）へ移植する。ログインキー（パスワード以外の代替認証手段）でのログインAPIを実装する。

対象エンドポイント:

| メソッド | パス | 概要 |
|---|---|---|
| POST | `/auth/login-key` | ログインキーによる認証、成功時はJWTトークンを返す |

## 説明

- リクエストボディ: `loginKey`
- `X-Tenant-ID` ヘッダー必須
- ログインキーとテナントIDでユーザーを検索し、該当ユーザーが存在しない場合は認証エラーとする
- 認証成功時、通常ログイン（`/auth/login`）と同じ `AuthResponse`（JWTトークン等）を返す

参照実装:

- Controller: `~/Documents/naw-server/src/main/java/com/example/naw/api/AuthController.java`（`authenticateWithLoginKey`, L144-169）
- Request: `~/Documents/naw-server/src/main/java/com/example/naw/request/LoginKeyRequest.java`
- Domain: `~/Documents/naw-server/src/main/java/com/example/naw/domain/user/User.java`（`loginKey`）、`UserRepository.java`（`findByLoginKeyAndTenantId`, L31）
- UserDetailsServiceImpl: `~/Documents/naw-server/src/main/java/com/example/naw/service/UserDetailsServiceImpl.java`（`loadUserByLoginKey`, L86-100）

## その他

- Issue本文には元チケット（NAW番号）参照は含まれていない。本対応は `issue-68`（NAWなしブランチ）として扱う
- 本タスクは自動化フロー（HITL省略・全自動モード・ブラウザ動作確認省略）で対応する
