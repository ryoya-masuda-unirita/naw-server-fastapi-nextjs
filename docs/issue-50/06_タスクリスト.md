# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/alembic/versions/013_add_shares.py` を作成し、`shares`/`share_rooms` の2テーブルを追加する
- [x] `backend/app/models/share.py` に `Share`・`ShareRoom` を追加する
- [x] `backend/app/schemas/share.py` にリクエスト・レスポンススキーマを追加する
- [x] `backend/app/repositories/share_repository.py` を追加する
- [x] `backend/app/repositories/share_room_repository.py` を追加する
- [x] `backend/app/services/share_service.py` を追加する
- [x] `backend/app/routers/shares.py` を追加し `backend/app/main.py` に登録する

## テスト

- [x] `backend/tests/integration/test_shares.py` を追加する（05のテストクラス構成に沿って実装）
- [x] `pytest` を実行し全テストが通ることを確認する
- [x] `mypy app/`・`ruff check app/` を実行し警告がないことを確認する

## 動作確認

- [x] ローカル環境（`uvicorn` + Docker PostgreSQL）でマイグレーションを適用する
- [x] `curl` で各エンドポイントを実際に呼び出して動作確認する（所有者・共有先グループメンバー2ユーザーでの確認を含む）
- [x] `08_動作確認.md` に確認結果を記録する

## PR・レビュー

- [ ] PR を作成する（`Closes #50` を本文に含める）
- [ ] `/code-review` を実行する
- [ ] `code-review.md` を作成する
- [ ] 指摘（🔴 致命的は必須）を修正し、再テスト・再動作確認する
