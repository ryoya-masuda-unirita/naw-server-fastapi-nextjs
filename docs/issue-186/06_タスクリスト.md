# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `pyproject.toml` に `aioboto3` を追加する
- [x] `docker-compose.yml` に `localstack` サービスを追加する
- [x] LocalStack起動時のバケット・キュー初期化スクリプトを追加する
- [x] `app/core/config.py` に `AwsSettings` を追加する
- [x] `.env.example` にAWS/LocalStack向け環境変数を追加する
- [x] `app/core/file_storage.py` に `S3FileStorage`・`get_user_import_file_storage()` を追加する（`LocalFileStorage`は変更しない）
- [x] `app/services/user_import_queue_service.py` を新規追加する
- [x] `app/services/user_import_listener.py` を新規追加する（`UserImportJobRepository.find_by_id`も追加）
- [x] `app/services/user_import_service.py` を非同期フロー（アップロード＋キュー送信のみ）に変更する
- [x] `app/main.py` に `lifespan` を追加し、起動時にリスナーのバックグラウンドタスクを開始する

## テスト

- [x] `tests/unit/test_file_storage.py` に `TestS3FileStorage` を追加する
- [x] `tests/unit/test_user_import_queue_service.py` を新規追加する
- [x] `tests/integration/test_user_import_listener.py` を新規追加する
- [x] `tests/integration/test_user_import.py` を非同期フローに合わせて書き換える
- [x] `pytest` を実行し全件パスすることを確認する（775 passed, 0 failed）

## 動作確認

- [x] `docker compose up` でLocalStackを含む全コンテナが起動することを確認する
- [x] APIを直接叩き、S3アップロード→SQS送信→受信→DB反映まで一連の流れが動作することを確認する（LocalStackのデフォルトリージョン(us-east-1)とbackend側のリージョン(ap-northeast-1)が不一致でNonExistentQueueになる不具合を発見し、初期化スクリプトに`--region`指定を追加して解消）
- [x] `AWS_SQS_LISTENER_ENABLED=false` 設定時に、登録APIはPENDINGのまま応答し、リスナーが起動しないことを確認する

## PR・レビュー

- [x] PR を作成（#190）
- [x] `/code-review` を実行し `code-review.md` を作成（🔴2件・🟡1件を修正、🔵1件は移植元踏襲のため見送り）
