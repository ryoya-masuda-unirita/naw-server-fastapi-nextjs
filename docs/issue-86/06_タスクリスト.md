# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/pyproject.toml` にAzure SDK依存（`azure-identity`, `azure-mgmt-costmanagement`）を追加
- [x] `backend/app/core/config.py` に `AzureCostSettings`・`get_azure_cost_settings()` を追加
- [x] `backend/app/core/azure_cost_client.py` を新規作成（`AzureCostQueryResult`, `AzureCostClient`）
- [x] `backend/app/repositories/tenant_resource_repository.py` に `find_by_id_and_tenant_id` を追加
- [x] `backend/app/schemas/tenant.py` に `TenantResourceCostResponse` を追加
- [x] `backend/app/services/tenant_service.py` に `query_cost` を追加
- [x] `backend/app/routers/tenants.py` に `GET /resources/{resource_id}/cost` を追加

## テスト

- [x] `backend/tests/unit/test_azure_cost_client.py` を新規作成
- [x] `backend/tests/unit/test_tenant_service.py` に `TestQueryCost` を追加
- [x] `backend/tests/integration/test_tenants.py` に `TestGetTenantResourceCost` を追加
- [x] `pytest`（全件）・`ruff check`・`ruff format --check`・`mypy`（新規追加分に新規エラーがないこと）を実行

## 動作確認

- [x] ブラウザ操作による動作確認は行わず、自動テスト（pytest 536件）の実行結果で代替（`08_動作確認.md`参照）

## PR・レビュー

- [x] PR を作成（#89）
- [x] `/code-review` を実行し `code-review.md` を作成
- [x] code-review指摘（🟡×2）を修正し、pytest 541件・ruff check・ruff format --check再実行で確認
