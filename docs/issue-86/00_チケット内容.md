# #86 issue-86 テナントリソースコスト取得API(管理者向け)を移植

## 概要

管理者向けのテナントリソースコスト取得API `GET /admin/tenants/resources/{resourceId}/cost` を移植元(Spring Boot)からFastAPIへ移植する。

## 説明

- 移植元（バックエンド）: `~/Documents/naw-server/src/main/java/com/example/naw/api/TenantController.java`（`getTenantResourceCost` メソッド、197行目付近）
- 移植元サービス: `~/Documents/naw-server/src/main/java/com/example/naw/service/AzureCostManageService.java`（Azure Cost Management SDK呼び出し）
- 移植元レスポンス: `TenantResourceCostResponse`（`com.example.naw.response` 配下）
- 移植先の現状: `backend/app/routers/tenant_endpoints.py` には `/{tenant_id}/endpoints` 系5エンドポイントのみ実装済みで、`/resources/{resourceId}/cost` は未実装

テナントに紐づくリソース（Azureリソース）のコスト情報を Azure Cost Management API 経由で取得するエンドポイントをFastAPI側に実装する。リクエストパラメータ（resourceId、期間指定等）・レスポンス形式は移植元の実装を踏襲する。認証済みテナントIDとリクエスト対象リソースの紐付けチェックなど、既存の仕組みを踏襲する。

## その他

`/naw-issue-batch` により自動検出・起票。本対応は全自動フロー（HITL承認省略、動作確認は自動テストで代替）で実施する。
