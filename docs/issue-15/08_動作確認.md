# 動作確認

## 前提条件

- `cd frontend && npm run dev` で開発サーバーを起動していること

---

## Step 1: ログイン画面の外観確認

**URL**: http://localhost:5173/auth/login

| # | 確認項目 | 期待値 | 結果 |
|---|---|---|---|
| 1 | SecuAIgent ロゴが表示される | 画像が正しく表示される | ✅ |
| 2 | 背景がグラデーション（薄青） | `#15819a33` の薄青グラデーション | ✅ |
| 3 | カードが中央配置・角丸（rounded-4xl） | 白いカードが中央に表示される | ✅ |
| 4 | 「ユーザーID」ラベルと入力フィールドが表示される | ラベル + プレースホルダー付きフィールド | ✅ |
| 5 | 「パスワード」ラベル + サポートテキストが表示される | 管理者に依頼するよう促すテキストが表示 | ✅ |
| 6 | パスワードフィールドに目のアイコンがある | 目のアイコンをクリックすると表示/非表示切り替わる | ✅ |
| 7 | ログインボタンにアイコンとテキストが横並び | `→|` アイコン + 「ログイン」テキスト | ✅ |
| 8 | 入力済みフィールドの背景が薄青になる | `bg-bg-brand-weak`（薄青）に変わる | ✅ |

---

## Step 2: ログイン画面の動作確認

**URL**: http://localhost:5173/auth/login

| # | 確認項目 | 操作 | 期待値 | 結果 |
|---|---|---|---|---|
| 1 | 空のまま送信 | 何も入力せずログインボタンをクリック | 送信されない（フィールドの required が機能する） | ✅ |
| 2 | バックエンド疎通なし時のエラー表示 | 認証情報を入力してログインボタンをクリック（バックエンド未起動） | 「予期しないエラーが発生しました。もう一度お試しください。」が表示される | ✅ |

---

## Step 3: パスワードリセット画面の外観確認

**URL**: http://localhost:5173/auth/pw-reset?username=testuser&oldPassword=oldpass&reason=INITIAL

| # | 確認項目 | 期待値 | 結果 |
|---|---|---|---|
| 1 | タイトルの下に INITIAL メッセージが表示される | ヘッダー内にサブテキストとして表示される | ✅ |
| 2 | username フィールドに `testuser` が自動入力される | クエリパラメータから自動填入 | ✅ |
| 3 | oldPassword フィールドに `oldpass` が自動入力される | クエリパラメータから自動填入 | ✅ |
| 4 | パスワードリセットボタンにアイコンとテキストが横並び | 🔒 アイコン + 「パスワードを変更する」テキスト | ✅ |

---

## Step 4: パスワードリセット画面の動作確認

**URL**: http://localhost:5173/auth/pw-reset?username=testuser&oldPassword=oldpass&reason=EXPIRED

| # | 確認項目 | 操作 | 期待値 | 結果 |
|---|---|---|---|---|
| 1 | EXPIRED メッセージが表示される | URL の reason=EXPIRED で確認 | ヘッダー内に EXPIRED サブテキスト | ✅ |
| 2 | パスワード不一致エラー | newPassword と confirmPassword に異なる値を入力して送信 | 「パスワードが一致しません」エラーが表示される | ✅ |
