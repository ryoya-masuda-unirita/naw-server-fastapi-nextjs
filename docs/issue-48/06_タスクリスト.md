# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/alembic/versions/012_add_messages.py` を作成し、`messages`/`message_contents`/`message_files`/`message_feedbacks` の4テーブルを追加する
- [x] `backend/app/models/message.py` に `Message`・`MessageContent`（`MessageContentStatus`）・`MessageFile`・`MessageFeedback`（`MessageRating`）を追加する
- [x] `backend/app/schemas/message.py` にリクエスト・レスポンススキーマ（`ToolConfig` を含む）を追加する
- [x] `backend/app/repositories/message_repository.py` を追加する
- [x] `backend/app/repositories/message_content_repository.py` を追加する
- [x] `backend/app/repositories/message_feedback_repository.py` を追加する
- [x] `backend/app/services/message_service.py` を追加する
- [x] `backend/app/routers/messages.py` を追加し `backend/app/main.py` に登録する

## テスト

- [x] `backend/tests/integration/test_messages.py` を追加する（05のテストクラス構成に沿って実装）
- [x] `pytest` を実行し全テストが通ることを確認する
- [x] `mypy app/`・`ruff check app/` を実行し警告がないことを確認する

## 動作確認

- [x] ローカル環境（`uvicorn` + Docker PostgreSQL）でマイグレーションを適用する
- [x] `curl` または `frontend-angular` で各エンドポイントを実際に呼び出して動作確認する
- [x] `08_動作確認.md` に確認結果を記録する

## PR・レビュー

- [x] PR を作成する（`Closes #48` を本文に含める）
- [x] `/code-review` を実行する
- [x] `code-review.md` を作成する
- [x] 指摘（🔴 致命的は必須）を修正し、再テスト・再動作確認する
