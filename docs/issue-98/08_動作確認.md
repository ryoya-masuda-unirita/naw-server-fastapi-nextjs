# 08_動作確認

## 確認環境

本Issueでは、ブラウザでの実操作確認は行わず、**自動テスト（pytest）の実行結果のみで動作確認を代替する**（`naw-issue-loop`スキルの全自動モードの方針による）。

## 確認手順

```bash
cd backend
uv run ruff check app/ tests/
uv run pytest tests/unit -q
```

## 確認結果

- `ruff check app/ tests/`: `All checks passed!`
- `pytest tests/unit -q`: `185 passed, 17 warnings in 10.25s`（回帰なし。既存テストはすべて成功し、本Issueで追加した以下のテストも成功していることを確認した。件数はcode-review対応（`error`/`response.failed`イベントのテスト追加）を反映した最終値）
  - `tests/unit/test_llm_client.py`: `_build_responses_tool`のツール変換テスト（web_search/mcp必須項目のみ/mcp全項目/未対応ツール名）、`AzureLlmChatClient.stream_chat`のResponses API呼び出し分岐テスト（tools未指定でChat Completions APIが呼ばれること・tools指定でResponses APIが呼ばれること・テキスト差分とトークン使用量のマッピング・max_output_tokensの条件付き付与・`error`/`response.failed`イベント受信時に例外を送出すること）
  - `tests/unit/test_llm_chat_service.py`: `tools`指定時に`AzureLlmChatClient.stream_chat`へ変換済み`tools`が渡ること、未指定時は`tools=None`が渡ること（回帰確認）
  - `tests/unit/test_message_service.py`: SAAS_CHAT・SAAS_RAGそれぞれで`tools`指定時に変換済み`tools`が渡ること、未指定時は`tools=None`が渡ること（回帰確認）
- `mypy`: 本Issueで変更した5ファイル（`app/core/llm_client.py` / `app/schemas/llm.py` / `app/schemas/message.py` / `app/services/llm_chat_service.py` / `app/services/message_service.py`）自体に起因するエラーは0件（他の既存ファイルに起因する既存のSQLAlchemy/SQLModel型付け関連エラーのみで、本Issueの変更前から存在するもの）

## 未確認事項

- Azure OpenAI（Responses API、web_search/mcp組み込みツール）への実際の疎通確認は、本環境にAzure OpenAIの実接続情報がないため未実施。単体テストではすべて`AsyncAzureOpenAI`をモックして検証している。
- Azure OpenAI Responses APIのAPIバージョン（`AZURE_OPENAI_RESPONSES_API_VERSION = "2025-04-01-preview"`）が実際のAzureテナント環境で有効かどうかは未検証。実際の疎通確認時にエラーとなる場合は、Microsoft公式ドキュメントを参照のうえ値を調整する必要がある。
- ブラウザ（`frontend-angular`）を用いたエンドツーエンドの動作確認は、本Issueのスコープでは実施していない（自動テストで代替）。
