# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] root `docker-compose.yml` に `redis` サービスを追加
- [x] `backend/pyproject.toml` に `redis`（本体）・`fakeredis`（dev）を追加
- [x] `backend/.env` / `backend/.env.example` に `REDIS_URL`・`COOKIE_SAME_SITE` を追加
- [x] `backend/app/core/config.py` に `RedisSettings`・`get_redis_settings()`、`Settings.cookie_same_site` を追加
- [x] `backend/app/core/redis_client.py` を新規作成（Redisクライアント・`get_redis_client` Depends）
- [x] `backend/app/core/session_store.py` を新規作成（セッションの作成・取得・削除・TTL延長）
- [x] `backend/app/core/security.py` を変更（Cookie方式をセッションID化、`get_current_user` を
      セッション優先＋bearerフォールバック方式に変更）
- [x] `backend/app/routers/auth.py` を変更（login/login-key/password-reset でセッション確立、
      logoutでセッション削除、`/api/auth` でセッションTTL延長）

## テスト

- [x] `backend/tests/integration/conftest.py` に `fake_redis`・`override_get_redis_client`
      フィクスチャを追加
- [x] `backend/tests/integration/test_auth.py` を更新（Cookie名変更・セッション検証ケース追加）
- [x] `backend/tests/unit/test_security.py` を更新（`set_access_token_cookie`等の参照が残っていた
      見落としを修正）
- [x] `backend/tests/integration/test_users.py` を更新（Cookieに生JWTを入れていたテストをセッション
      ベースに更新、`client` フィクスチャに `override_get_redis_client` を追加）
- [x] `uv run pytest` で全テスト成功を確認（748 passed）
- [x] `uv run mypy app` で型エラーがないことを確認（変更・新規ファイル自体にエラーなし。既存の
      無関係なエラーは対象外）
- [x] `uv run ruff check app` でlintエラーがないことを確認

## 動作確認

- [ ] root で `docker compose up -d redis` を実行しRedisが起動することを確認
- [ ] backendをローカル起動し、frontend-angular（`http://localhost:4201`）からログインできることを確認
- [ ] ログイン後、Redis内に `session:*` キーが作成されていることを確認（`redis-cli` 等）
- [ ] ログアウト後、該当セッションキーが削除されることを確認
- [ ] 動作確認を `08_動作確認.md` に記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
