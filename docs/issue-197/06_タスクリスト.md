# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [ ] `backend/Dockerfile` に `alembic.ini` と `alembic/` のCOPYを追加する
- [ ] `infra/ecs.tf` にマイグレーション専用タスク定義 `aws_ecs_task_definition.backend_migrate`（ポートマッピング無し・コマンド固定・小メモリ）と、対応するCloudWatchロググループを追加する
- [ ] `infra/github_oidc.tf` の `EcsDeploy` に `ecs:RunTask`・`ecs:DescribeTasks` を追加する
- [ ] `infra/` で `terraform fmt` / `terraform validate` を実行する
- [ ] `.github/workflows/deploy-backend.yml` に、マイグレーションタスク定義の更新・実行・完了待ち・exitCode検査のステップを追加する
- [ ] `infra/scripts/seed_via_bastion.sh` を新規作成する（terraform outputからの接続先自動取得、SSHトンネル確立、マイグレーション適用、seed投入、トンネルの後始末）
- [ ] `infra/README.md` の該当手順をスクリプト実行手順に置き換える

## テスト

- [ ] `terraform apply` でマイグレーション専用タスク定義・IAM権限変更をdev環境に反映する
- [ ] `04_テスト設計.md` のNo.1〜7を `05_テスト詳細設計.md` の手順で実施する

## 動作確認

- [ ] `08_動作確認.md` に実施結果を記録する

## PR・レビュー

- [ ] PR を作成
