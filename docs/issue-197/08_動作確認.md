# 08_動作確認

## 確認環境

- AWS dev環境（`naw-fastapi-issue160`、`terraform apply`で73リソースを新規作成済み）
- GitHub Actions（このリポジトリ）

## 確認手順・確認結果

### 静的チェック

```bash
cd infra && terraform fmt -check && terraform validate
```
→ 成功（差分なし、`Success! The configuration is valid.`）

```bash
bash -n infra/scripts/seed_via_bastion.sh
```
→ 構文エラーなし

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-backend.yml'))"
```
→ 成功（YAML構文エラーなし）

### CI/CD実機テスト（`04_テスト設計.md` No.1〜7）について

`infra/github_oidc.tf`のIAMロール信頼ポリシーが、OIDCの`sub`クレームを`repo:...:ref:refs/heads/develop`に厳密限定している（このリポジトリの既存の意図的な設計）。そのため、`feature/issue-197`ブランチ上で`deploy.yml`を`workflow_dispatch`で手動起動したところ、マイグレーション実行以前の`Configure AWS credentials`ステップで認証エラーとなり、テストできなかった。

```
##[error]Could not assume role with OIDC: Not authorized to perform sts:AssumeRoleWithWebIdentity
```
（実行ログ: https://github.com/ryoya-masuda-unirita/naw-server-fastapi-nextjs/actions/runs/30695723000）

この制約により、`04_テスト設計.md`のNo.1〜7（マイグレーション自動実行・失敗時の後続停止・seedスクリプト実機確認等）は、**`develop`ブランチへのマージ後に実施する**方針とした。マージ前に用意していたダミー失敗マイグレーション（No.2用）は、`develop`にそのまま持ち込むと通常デプロイを壊すため、マージ前に削除済み。

## 未確認事項

- `04_テスト設計.md` No.1〜7 は全て未実施（上記の理由により`develop`マージ後に実施予定）
- マージ後、実際に`deploy-backend.yml`のマイグレーションタスクが成功することの確認
- マージ後、意図的に失敗するマイグレーションを一時的に投入し、後続のbackendサービス更新に進まないことの確認（確認後は速やかに元に戻す）
- `infra/scripts/seed_via_bastion.sh`の実機確認（bastionのPEM鍵が必要なため、ユーザー環境での実施が必要）
