# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/pyproject.toml` の `[tool.mypy]` に `files`・`python_version`・`warn_unused_ignores`・`warn_redundant_casts` を追加
- [x] `app.repositories.*`・`app.services.user_service`・`app.services.user_import_service`・`app.core.credit_quota` を対象に、理由をコメントした `[[tool.mypy.overrides]]`（`ignore_errors = true`）を追加
- [x] `warn_unused_ignores` により検出された `tests/unit/test_azure_cost_client.py` の不要な `# type: ignore` を削除

## テスト

- [x] `uv run mypy app tests` を実行し、対象範囲・除外設定・残るエラー件数（2件）が設計通りであることを確認
- [x] `uv run pytest tests/unit -q` を実行し、既存テストが全件成功することを確認

## 動作確認

- [x] `08_動作確認.md` に、自動テスト・mypy実行結果による確認内容を記録（本Issueはブラウザ操作を伴わない設定変更のため、ブラウザでの動作確認は対象外）

## PR・レビュー

- [x] PR を作成（`Closes #140`）
- [x] `/code-review` を実行し `code-review.md` を作成
