# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `GroupAssistantRepository.find_grouped_by_group_ids` を追加
- [x] `GroupPromptTemplateRepository.find_grouped_by_group_ids` を追加
- [x] `GroupListItemResponse` のコメント・デフォルト値を修正
- [x] `GroupService._to_list_item` をマップ受け取り方式に変更し、`get_groups`/`list_groups` を修正

## テスト

- [x] Router統合テスト（`tests/integration/test_groups.py`）に実データ配線を検証するテストを追加
      （`GroupAssistantRepository`/`GroupPromptTemplateRepository`専用のテストファイルは既存コードベースに存在せず、
      router統合テスト経由で実DBを通して新規リポジトリメソッドも検証する既存方針を踏襲）

## 動作確認

- [x] 自動テスト（pytest）の実行結果で代替し、結果を記録（`08_動作確認.md`参照）

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
