# 08_動作確認

本Issueはブラウザでの実操作確認は行わず、自動テスト（pytest）の実行結果で代替する（`naw-issue-loop`実行方針による）。

## 確認環境

- ローカル: `naw-fastapi-postgres`（Docker）に接続した状態で `backend/` の pytest を実行

## 確認手順

```bash
cd backend
uv run pytest tests/integration/test_groups.py tests/unit/test_group_service.py -q
uv run pytest -q
uv run ruff check app/ tests/integration/test_groups.py
uv run mypy app/
```

## 確認結果

- `tests/integration/test_groups.py` / `tests/unit/test_group_service.py`: 61 passed
- 全体テストスイート（`pytest -q`）: 717 passed
- `ruff check`: All checks passed!
- `mypy app/`: 変更前から存在するSQLModel×mypyの相性起因のエラー（`select()`のオーバーロード解決失敗等）が既存のまま残っているのみで、本Issueの変更による新規エラーはないことを`git stash`比較で確認済み

追加した統合テスト（`tests/integration/test_groups.py` の `TestGetGroups`）:
- `test_returns_assistants_and_prompt_templates`: 紐づくアシスタント・プロンプトテンプレートの実データがレスポンスに含まれることを確認
- `test_returns_empty_lists_when_nothing_linked`: 何も紐づいていない場合は空リストのままであることを確認
- `test_multiple_groups_do_not_mix_assistant_data`: 複数グループの一覧取得で紐付けデータが混在しないことを確認

## 未確認事項

- ブラウザ（`frontend-angular/`）での実際の画面表示確認は実施していない（自動テストでの代替のため）
