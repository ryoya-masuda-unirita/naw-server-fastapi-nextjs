# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/Dockerfile.dev` を新規作成
- [x] `frontend-angular/Dockerfile.dev` を新規作成
- [x] `frontend-angular/proxy.conf.local.js` を環境変数で転送先を切り替え可能に変更
- [x] `docker-compose.yml` に `backend`・`frontend-angular` サービスを追加

## テスト

- 自動テストなし（`05_テスト詳細設計.md`参照）

## 動作確認

- [ ] `docker compose up` で4サービス（postgres/redis/backend/frontend-angular）が起動することを確認
- [ ] backendのホットリロードを確認（`app/`配下編集→再起動なしで反映）
- [ ] frontend-angularのホットリロードを確認（`src/`配下編集→再起動なしで反映）
- [ ] ブラウザ（`http://localhost:4201`）からログインし、backendとの疎通を確認
- [ ] ホストでの直接起動フロー（`uvicorn --reload`・`npm start`）に影響がないことを確認
- [ ] 動作確認を `08_動作確認.md` に記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
