# 06_タスクリスト

## 実装

- [ ] `backend/app/models/tenant_endpoint.py` に `EndpointType`・`TenantEndpoint` モデルを追加する
- [ ] `backend/alembic/versions/005_add_tenant_endpoints.py` を作成し `tenant_endpoints` テーブルを追加する
- [ ] `backend/app/repositories/tenant_endpoint_repository.py` を新規作成する
- [ ] `backend/app/schemas/tenant_endpoint.py` を新規作成する
- [ ] `backend/app/services/tenant_endpoint_service.py` を新規作成する（LOCAL_SERVER限定バリデーション含む）
- [ ] `backend/app/core/security.py` に `require_admin_for_tenant_path` を追加する
- [ ] `backend/app/routers/tenant_endpoints.py` を新規作成する
- [ ] `backend/app/main.py` に新規ルーターを登録する

## テスト

- [ ] `tests/integration/conftest.py` のテーブルクリア対象に `tenant_endpoints` を追加する
- [ ] `tests/integration/test_tenant_endpoints.py` を新規作成する
- [ ] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## マイグレーション適用・動作確認

- [ ] `alembic upgrade head` でマイグレーションを適用する
- [ ] `upgrade→downgrade→upgrade`を実行しEnum型の冪等性を確認する
- [ ] API直叩き（curl等）で作成・一覧・更新・削除、LOCAL_SERVER以外への操作拒否を確認する
- [ ] レスポンスに`apiKey`が含まれないことを確認する

## PR作成・レビュー

- [ ] PR作成（`--base develop`）
- [ ] `/code-review` 実行
- [ ] `docs/issue-28/code-review.md` 作成
- [ ] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- LOCAL_SERVER限定バリデーション・権限モデル・APIキー非開示をAPI直叩きで実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
