# 06_タスクリスト

## 実装

- [x] `backend/app/models/tenant_endpoint.py` に `EndpointType`・`TenantEndpoint` モデルを追加する
- [x] `backend/alembic/versions/005_add_tenant_endpoints.py` を作成し `tenant_endpoints` テーブルを追加する
- [x] `backend/app/repositories/tenant_endpoint_repository.py` を新規作成する
- [x] `backend/app/schemas/tenant_endpoint.py` を新規作成する
- [x] `backend/app/services/tenant_endpoint_service.py` を新規作成する（LOCAL_SERVER限定バリデーション含む）
- [x] `backend/app/core/security.py` に `require_admin_for_tenant_path` を追加する
- [x] `backend/app/routers/tenant_endpoints.py` を新規作成する
- [x] `backend/app/main.py` に新規ルーターを登録する

## テスト

- [x] `tests/integration/conftest.py` のテーブルクリア対象に `tenant_endpoints` を追加する
- [x] `tests/integration/test_tenant_endpoints.py` を新規作成する
- [x] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## マイグレーション適用・動作確認

- [x] `alembic upgrade head` でマイグレーションを適用する
- [x] `upgrade→downgrade→upgrade`を実行しEnum型の冪等性を確認する
- [x] API直叩き（curl等）で作成・一覧・更新・削除、LOCAL_SERVER以外への操作拒否を確認する
- [x] レスポンスに`apiKey`が含まれないことを確認する

## PR作成・レビュー

- [x] PR作成（`--base develop`）
- [x] `/code-review` 実行
- [x] `docs/issue-28/code-review.md` 作成
- [x] 指摘修正（🔴🟡致命的・注意な指摘なし、🔵提案3件は対応しない判断。コード修正なし、再テスト不要）

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- LOCAL_SERVER限定バリデーション・権限モデル・APIキー非開示をAPI直叩きで実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
