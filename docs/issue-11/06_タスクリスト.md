# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成（第1承認取得）
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/app/repositories/user_repository.py` を新規作成（`UserRepository.find_by_login_id`）
- [x] `backend/app/repositories/tenant_repository.py` を新規作成（`TenantRepository.find_by_id`）
- [x] `backend/app/core/security.py` を変更（`get_current_user` をリポジトリ利用に置き換え、`verify_password_async` を追加）
- [x] `backend/app/services/auth_service.py` を変更（リポジトリ利用への置き換え、`verify_password_async` への置き換え、`_get_tenant` 削除）
- [x] `backend/app/core/password_policy.py` を変更（`check_password_not_reused` を `verify_password_async` 利用に変更）
- [x] `backend/app/services/user_service.py` を変更
  - [x] `_get_user`／`_get_tenant` をリポジトリ利用に置き換え
  - [x] `_escape_like_pattern` を追加し `get_users` の検索条件に適用
  - [x] `_SORTABLE_COLUMNS`／`_resolve_sort_column` を追加し `get_users` のソート条件に適用
  - [x] `_issue_initial_password` を追加し `create_user`／`update_user` の重複処理を解消
- [x] `backend/app/schemas/user.py` を変更（`UserResponse.from_user` に型ヒントを付与）
- [x] コードベース全体（`backend/app/`配下）の型ヒント・`Optional`記法の規約違反を洗い出し修正
  - [x] `backend/app/core/security.py`: `get_current_user`の`credentials`引数に`HTTPAuthorizationCredentials`型ヒントを付与
  - [x] `backend/app/routers/auth.py`: `logout()`に戻り値型ヒント`dict[str, str]`を付与
  - [x] `backend/app/models/password_history.py`: `Optional[datetime]`を`datetime | None`に変更
  - [x] `backend/app/schemas/auth.py`: `Optional[str]`を`str | None`に変更（2箇所）

## テスト

- [x] `backend/tests/unit/test_user_service.py` にテストケースを追加（`TestEscapeLikePattern`／`TestResolveSortColumn`／`TestIssueInitialPassword`）
- [x] `backend/tests/unit/test_auth_service.py` にテストケースを追加（履歴0件ログイン失敗、リセット時の履歴保存検証）
- [x] `backend/tests/unit/test_security.py` に `TestVerifyPasswordAsync` を追加
- [x] `backend/tests/integration/test_user_repository.py` を新規作成
- [x] `backend/tests/integration/test_tenant_repository.py` を新規作成
- [x] `backend/tests/integration/test_users.py` に `TestGetUsersSearchAndSort` を追加
- [x] `pytest tests/unit -v` を実行し全て通ることを確認（46件成功）
- [x] `pytest tests/integration -v` を実行し全て通ることを確認（既存の不具合 `test_auth.py`・`test_models.py` の一部失敗を除き18件成功。これらは本Issue対象外の既存不具合であることをstashして確認済み）
- [x] 既存テストが内部実装変更の影響を受けて失敗していないか確認（影響なし）
- [x] `mypy` と `ruff` を dev 依存（`pyproject.toml`）に追加し `uv sync` でインストール
- [x] `ruff check app/` を実行（不要import `schemas/user.py` の `import math` を検出・修正し、全チェック通過を確認）
- [x] `mypy app/` を実行（`[tool.mypy] plugins = ["pydantic.mypy"]` 追加で `core/config.py` の誤検知を解消、`types-passlib` 追加で型スタブ不足を解消、自作の `_SORTABLE_COLUMNS` の型ヒントを `Any` に修正。21件→9件に削減。残り9件はSQLModelが`Mapped[]`注釈を使わないことに起因する構造的な既知の問題で、issue-11以前から存在する全`.where()`パターンに共通するため本Issueでは対応しない）

## 動作確認

- [x] サーバーを起動し、管理者ユーザーで `%` や `_` を含む検索文字列でユーザー一覧を検索し、ワイルドカードとして機能しないことを実機確認
- [x] `sort=name,asc` 等のキャメルケースのソート指定で正しくソートされることを実機確認
- [x] 許可リスト外のソート指定を行ってもエラーにならないことを実機確認
- [x] ユーザー作成・更新（パスワードリセット含む）・削除・プロフィール取得/更新が従来どおり動作することを実機確認
- [x] `08_動作確認.md` に実機確認結果を記録

## PR・レビュー

- [x] PRを作成（ベースブランチ: `develop`）→ PR #13
- [x] `/code-review` を実行
- [x] `code-review.md` を作成し、指摘内容・対応方針に加えて本Issue調査済みの認証エラーコード差異・削除冪等設計の調査結果を記録する
- [x] 🔴致命的指摘（`reset_password` の `expired_at` 欠落）を修正し再テスト（46件通過）
- [ ] PRの説明（Description）にテスト結果・動作確認結果を記載
