# 03_詳細設計

## 変更ファイル一覧

| ファイルパス | 種別 | 内容 |
|---|---|---|
| `backend/app/repositories/user_repository.py` | 新規 | ユーザー取得処理を集約するリポジトリ |
| `backend/app/repositories/tenant_repository.py` | 新規 | テナント取得処理を集約するリポジトリ |
| `backend/app/core/security.py` | 変更 | `get_current_user` をリポジトリ利用に変更、`verify_password_async` を追加 |
| `backend/app/core/password_policy.py` | 変更 | パスワード照合を非同期化 |
| `backend/app/services/user_service.py` | 変更 | リポジトリ利用への置き換え、LIKEエスケープ、ソート許可リスト、パスワード発行処理の共通化 |
| `backend/app/services/auth_service.py` | 変更 | リポジトリ利用への置き換え、パスワード照合を非同期化 |
| `backend/app/schemas/user.py` | 変更 | `UserResponse.from_user` に型ヒントを付与 |

## sort パラメータの実際の送信値（調査結果）

移植元フロントエンド（`secuaigent-client/src/app/features/admin/management/user-list/services/user-list-api.service.ts`）の調査により、`sort` パラメータは `"{sortField},{sortOrder}"` 形式で、`sortField` は **キャメルケース**（`name` / `role` / `updatedAt`）がそのまま送信されることを確認した。一方、既存のFastAPI実装・既存テストはルータのデフォルト値・テストケースともに `created_at,desc`（スネークケース）を使用している。

許可リストはキャメルケース・スネークケースの両方を受理できるよう定義し、既存の挙動（デフォルト値・既存テスト）を変更せずに、実際にフロントエンドから送られる値（`name` / `role` / `updatedAt`）にも正しく対応できるようにする。

## 1. リポジトリ層の新規追加

### `backend/app/repositories/user_repository.py`（新規）

```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.user import User


class UserRepository:

    @staticmethod
    async def find_by_login_id(login_id: str, tenant_id: str, session: AsyncSession) -> User | None:
        """loginId と tenantId でユーザーを取得する。

        Args:
            login_id: ログインID。
            tenant_id: テナントID。
            session: 非同期DBセッション。

        Returns:
            該当する User。存在しない場合は None。
        """
        stmt = select(User).where(User.login_id == login_id, User.tenant_id == tenant_id)
        result = await session.execute(stmt)
        return result.scalars().first()
```

### `backend/app/repositories/tenant_repository.py`（新規）

```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.tenant import Tenant


class TenantRepository:

    @staticmethod
    async def find_by_id(tenant_id: str, session: AsyncSession) -> Tenant | None:
        """テナントIDでテナントを取得する。

        Args:
            tenant_id: テナントID。
            session: 非同期DBセッション。

        Returns:
            該当する Tenant。存在しない場合は None。
        """
        stmt = select(Tenant).where(Tenant.id == tenant_id)
        result = await session.execute(stmt)
        return result.scalars().first()
```

存在しない場合の HTTPException 送出はサービス層の責務として残す（リポジトリ層は DB アクセスに専念する CLAUDE.md の規約に従うため）。

## 2. `backend/app/core/security.py` の変更

`get_current_user` 内の直書きクエリを `UserRepository.find_by_login_id` に置き換える。

```python
from app.repositories.user_repository import UserRepository

async def get_current_user(
    credentials=Depends(security),
    session: AsyncSession = Depends(get_session),
) -> User:
    token = credentials.credentials
    payload = decode_token(token)
    login_id = payload.get("sub")
    tenant_id = payload.get("tenantId")

    if not login_id or not tenant_id:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")

    user = await UserRepository.find_by_login_id(login_id, tenant_id, session)
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")

    return user
```

bcrypt 照合のブロッキング解消のため、非同期ラッパーを追加する。

```python
import asyncio

async def verify_password_async(plain_password: str, hashed_password: str) -> bool:
    """パスワードを別スレッドで検証し、イベントループの占有を防ぐ。

    bcrypt の検証処理は CPU バウンドな同期処理のため、async 関数内で直接呼び出すと
    イベントループを占有し他のリクエスト処理を遅延させる。asyncio.to_thread で
    別スレッドに退避させる。

    Args:
        plain_password: 検証対象の平文パスワード。
        hashed_password: 比較対象のハッシュ化済みパスワード。

    Returns:
        パスワードが一致すれば True。
    """
    return await asyncio.to_thread(verify_password, plain_password, hashed_password)
```

`hash_password`（パスワード生成時のハッシュ化）は本Issueの指摘対象（パスワード**照合**のブロッキング）には含まれないため、スコープ外として同期のまま維持する。

## 3. `backend/app/core/password_policy.py` の変更

`check_password_not_reused` の照合処理を非同期版に置き換える。

```python
from app.core.security import verify_password_async

async def check_password_not_reused(
    user_id: uuid.UUID,
    new_password: str,
    tenant: Tenant,
    session: AsyncSession,
) -> None:
    recent_hashes = await PasswordHistoryRepository.get_recent_hashes(
        user_id, tenant.pw_histories_limit, session
    )
    for hashed in recent_hashes:
        if await verify_password_async(new_password, hashed):
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail="Password has been used before",
            )
```

## 4. `backend/app/services/user_service.py` の変更

### 4-1. リポジトリ利用への置き換え

```python
from app.repositories.tenant_repository import TenantRepository
from app.repositories.user_repository import UserRepository

@staticmethod
async def _get_user(login_id: str, tenant_id: str, session: AsyncSession) -> User:
    user = await UserRepository.find_by_login_id(login_id, tenant_id, session)
    if not user:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="User not found")
    return user

@staticmethod
async def _get_tenant(tenant_id: str, session: AsyncSession) -> Tenant:
    tenant = await TenantRepository.find_by_id(tenant_id, session)
    if not tenant:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Tenant not found")
    return tenant
```

`create_user` の重複チェック、`delete_user` の対象取得も `UserRepository.find_by_login_id` を使うように置き換える。

### 4-2. LIKE 検索のワイルドカードエスケープ

```python
@staticmethod
def _escape_like_pattern(value: str) -> str:
    """LIKE検索のワイルドカード文字（% _ \\）をエスケープし前後に%を付与する。

    Args:
        value: エスケープ対象の検索文字列。

    Returns:
        LIKE検索にそのまま使用できるエスケープ済みパターン文字列。
    """
    escaped = value.replace("\\", "\\\\").replace("%", "\\%").replace("_", "\\_")
    return f"%{escaped}%"
```

`get_users` 内の該当箇所:

```python
if search_text:
    pattern = UserService._escape_like_pattern(search_text.lower())
    stmt = stmt.where(
        or_(
            func.lower(User.login_id).like(pattern, escape="\\"),
            func.lower(User.name).like(pattern, escape="\\"),
        )
    )
```

### 4-3. ソート対象列の許可リスト化

```python
from sqlalchemy.orm import InstrumentedAttribute

_SORTABLE_COLUMNS: dict[str, InstrumentedAttribute] = {
    "created_at": User.created_at,
    "createdAt": User.created_at,
    "login_id": User.login_id,
    "loginId": User.login_id,
    "name": User.name,
    "role": User.role,
    "updated_at": User.updated_at,
    "updatedAt": User.updated_at,
}

@staticmethod
def _resolve_sort_column(sort_col_name: str) -> InstrumentedAttribute:
    """ソート対象列名を許可リストに基づいてモデル属性に解決する。

    許可リスト外の列名が指定された場合は、任意の内部属性へのソートを防ぐため
    既定の作成日時列にフォールバックする。

    Args:
        sort_col_name: リクエストで指定されたソート対象列名。

    Returns:
        ソートに使用するモデル属性。
    """
    return UserService._SORTABLE_COLUMNS.get(sort_col_name, User.created_at)
```

`get_users` 内の該当箇所:

```python
sort_parts = sort.split(",")
sort_col_name = sort_parts[0] if sort_parts else "created_at"
sort_dir = sort_parts[1] if len(sort_parts) > 1 else "asc"
col = UserService._resolve_sort_column(sort_col_name)
stmt = stmt.order_by(col.desc() if sort_dir == "desc" else col.asc())
```

### 4-4. パスワード発行処理の共通化

`create_user` と `update_user`（resetPassword=True 時）に重複していたパスワード生成・有効期限算出・履歴保存の処理を共通メソッドに切り出す。

```python
@staticmethod
async def _issue_initial_password(
    user: User, tenant: Tenant, session: AsyncSession
) -> tuple[str, datetime]:
    """初期パスワードを生成し、パスワード履歴へ保存する。

    Args:
        user: 対象ユーザー。
        tenant: パスワードポリシーを持つテナント。
        session: 非同期DBセッション。

    Returns:
        生成した平文パスワードと有効期限のタプル。
    """
    plain_password = UserService._generate_initial_password(tenant)
    expired_at = datetime.now(timezone.utc) + timedelta(days=tenant.pw_validity_period_days)
    await PasswordHistoryRepository.save(
        user.id, tenant.id, hash_password(plain_password), session, expired_at=expired_at
    )
    return plain_password, expired_at
```

`create_user`:

```python
tenant = await UserService._get_tenant(tenant_id, session)
user = User(
    login_id=req.loginId,
    name=req.name,
    role=req.role,
    login_key=req.loginKey,
    tenant_id=tenant_id,
    is_required_password_reset=True,
)
session.add(user)
await session.flush()

plain_password, expired_at = await UserService._issue_initial_password(user, tenant, session)
await session.commit()
await session.refresh(user)
```

`update_user`:

```python
plain_password = None
expired_at = None

if req.resetPassword:
    tenant = await UserService._get_tenant(tenant_id, session)
    plain_password, expired_at = await UserService._issue_initial_password(user, tenant, session)
    user.is_required_password_reset = True
```

### 4-5. excludeGroupId の扱い（実装変更なし）

既存のコメント `# excludeGroupId はグループ機能実装時に対応する` を維持し、現状の「受理するが無視する」挙動を変更しない。設計意図は本ドキュメントと `02_基本設計.md` に明文化済み。

## 5. `backend/app/services/auth_service.py` の変更

```python
from app.core.security import create_access_token, hash_password, verify_password_async
from app.repositories.tenant_repository import TenantRepository
from app.repositories.user_repository import UserRepository


class AuthService:

    @staticmethod
    async def login(username: str, password: str, tenant_id: str, session: AsyncSession) -> AuthResponse:
        user = await UserRepository.find_by_login_id(username, tenant_id, session)
        if not user:
            raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid credentials")

        latest = await PasswordHistoryRepository.get_latest(user.id, session)
        if not latest:
            raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid credentials")

        if not await verify_password_async(password, latest.password):
            raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid credentials")

        # ...(以降は変更なし)

    @staticmethod
    async def reset_password(
        login_id: str, old_password: str, new_password: str, tenant_id: str, session: AsyncSession
    ) -> AuthResponse:
        user = await UserRepository.find_by_login_id(login_id, tenant_id, session)
        if not user:
            raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="User not found")

        latest = await PasswordHistoryRepository.get_latest(user.id, session)
        if not latest or not await verify_password_async(old_password, latest.password):
            raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Invalid old password")

        tenant = await TenantRepository.find_by_id(tenant_id, session)
        if not tenant:
            raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Tenant not found")

        # ...(以降は変更なし)
```

`AuthService._get_tenant`（プライベートメソッド）は `TenantRepository.find_by_id` に置き換わるため削除する。

ステータスコード（login: 401、reset_password: 403）は変更しない。移植元 Spring Boot 実装（`UserDetailsServiceImpl` → `AuthService` 経由で401、`PasswordResetService` で403に統一）と一致する意図的な設計であることを調査により確認済み（詳細は「6. 調査結果の記録」を参照）。

## 6. `backend/app/schemas/user.py` の変更

```python
from app.models.user import User, UserRole

class UserResponse(BaseModel):
    ...

    @classmethod
    def from_user(cls, user: User) -> "UserResponse":
        return cls(
            id=str(user.id),
            loginId=user.login_id,
            name=user.name,
            role=user.role,
            loginKey=user.login_key,
            isRequiredPasswordReset=user.is_required_password_reset,
        )
```

## 7. 調査結果の記録（実装変更なし）

以下2点は移植元 Spring Boot 実装を調査した結果、いずれも意図的な設計と一致していることを確認した。実装は変更せず、本調査結果を `code-review.md` に記録する。

### 7-1. ログインAPI（401）とパスワードリセットAPI（403）のステータスコード差異

Spring Boot 元実装では、ログイン認証失敗（パスワード履歴0件を含む）は `JwtAuthenticationEntryPoint` 経由で一貫して **401** を返す設計、パスワードリセットAPI（`PasswordResetService`）はユーザー不在・パスワード履歴0件・旧パスワード不一致のいずれも **403** を返す設計で、コード内コメントにも「要件に従い403とする」と明記されている。FastAPI版の現状実装（login: 401、reset_password: 403）はこの設計と一致しており、issue-9のリファクタで生じた意図しない変化ではない。

### 7-2. ユーザー削除APIの冪等設計

Spring Boot 元実装の `UserService.deleteUser` は `deleteByLoginIdAndTenantId` という Spring Data JPA の派生削除クエリを使用しており、対象が0件でも例外を投げず、事前の存在チェックも行わない。FastAPI版の現状実装（対象不在時に何もせず204を返す）はこの設計と一致した意図的な冪等設計である。
