# #101 issue-101 メッセージ再生成(messageContentIdによる既存回答上書き)対応

## 概要

`messageContentId`を指定して既存のアシスタント回答を再生成・上書きする機能に対応する。

## 説明

issue-93（メッセージ送信API移植）でスコープ外として明記されていた機能。

- 移植元（バックエンド）: `~/Documents/naw-server/src/main/java/com/example/naw/service` 配下の再生成関連実装
- 移植先: `backend/app/services/message_service.py`, `backend/app/models/message.py`
- 関連Issue: #93（スコープ外節を参照）
- 参照調査: `docs/調査結果/20260711.md`（4章 項目6）

## その他

- スコープ（想定）: `messageContentId`指定時の既存MessageContentの再生成・上書き
