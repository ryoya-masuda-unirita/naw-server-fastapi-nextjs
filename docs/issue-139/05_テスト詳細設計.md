# 05_テスト詳細設計

## 対象コマンド

| No | コマンド | 期待結果 |
|---|---|---|
| 1 | `cd backend && uv run mypy app tests` | `Found 435 errors in 37 files (checked 195 source files)`（`03_詳細設計.md` の集計と一致） |
| 2 | `cd backend && uv run pytest tests/unit -q` | 既存の unit テストが全件成功する（ソースコード変更なしのため既存の合否から変化しないことの確認） |

## 備考

- 本 Issue はコード変更を行わないため、新規テストの追加は不要
- 上記2つのコマンドの実行結果は `08_動作確認.md` に記録する
