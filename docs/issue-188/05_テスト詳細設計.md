# 05_テスト詳細設計

## 実行コマンドと対象

| No | 対応する04のNo | コマンド | 実行ディレクトリ |
|---|---|---|---|
| 1 | 1 | `terraform validate` | `infra/` |
| 2 | 2 | `terraform fmt -check -diff` | `infra/` |
| 3 | 3, 4 | `terraform plan -out=/dev/null`（AWS認証情報が必要。ユーザーのプロファイル`naw-fastapi-issue158`を利用） | `infra/` |
| 5 | 5 | `infra/iam.tf` の `aws_iam_role_policy.ecs_task_sqs_s3` を目視確認 | - |

## 実行者

- No.1, 2, 5: Claudeが実行・確認する
- No.3: `terraform plan`はAWS認証情報（`profile = "naw-fastapi-issue158"`）を使ってAWSと通信するため、実行前にユーザーへ実行してよいか確認する。読み取り専用操作（`plan`はAWSリソースを変更しない）だが、AWS実環境との通信を伴うため事前確認する
