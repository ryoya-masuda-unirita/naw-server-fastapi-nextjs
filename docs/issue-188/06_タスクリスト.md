# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `infra/sqs.tf` を新規作成（メインキュー・DLQ）
- [x] `infra/s3_user_import.tf` を新規作成（ユーザーインポート用バケット・パブリックアクセスブロック）
- [x] `infra/iam.tf` に `backend_task` ロール・インラインポリシーを追記
- [x] `infra/ecs.tf` に `task_role_arn` と環境変数2件を追加
- [x] `infra/outputs.tf` にキューURL・バケット名の出力を追加

## テスト

- [x] `terraform fmt -check -diff` を実行
- [x] `terraform validate` を実行
- [x] IAMポリシーのスコープ（ワイルドカード不使用）を目視確認
- [x] `terraform plan` を実行（実行前にユーザーへ確認）

## 動作確認

- [x] `terraform plan` の出力内容を `08_動作確認.md` に記録

## PR・レビュー

- [x] PR を作成（`Closes #187 #188`）
- [x] `/code-review` を実行し `code-review.md` を作成
