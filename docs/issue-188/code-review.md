# code-review 結果

対象: PR #191（`infra/` へのTerraform化: SQS/S3/IAM）

## 指摘一覧

指摘事項なし（致命的なバグ・条件反転・null参照・エラー握り潰し等は検出されなかった）。

## 詳細

### 補足事項（フラグ対象外）

`infra/sqs.tf`のDLQ側で`redrive_allow_policy`（「どのキューからのリドライブを許可するか」をDLQ側から制限する設定）が未定義、との補足があった。

**対応しない**: バグではなく任意設定のため。未設定の場合AWSのデフォルト挙動（同一アカウント内の全キューからのリドライブを許可）となり、今回のようにメインキュー1つからのみDLQへ転送する構成では実害がない。将来同一プロジェクト内に別のSQSキューが増えた場合の防御的な設定として検討の余地はあるが、本チケットのスコープ（#186実装分のSQS/S3をIaC化する）を超えるため見送る。
