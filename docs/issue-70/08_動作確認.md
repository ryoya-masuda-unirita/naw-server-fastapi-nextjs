# 08_動作確認

## 確認環境

- `/naw-issue-loop` 全自動モード
- ブラウザ操作は行わず、自動テストで代替

## 確認手順

```bash
cd backend
uv --cache-dir /tmp/uv-cache run ruff check app/repositories/group_assistant_repository.py app/routers/groups.py app/schemas/assistant.py app/schemas/group.py app/services/group_service.py tests/integration/test_groups.py
uv --cache-dir /tmp/uv-cache run ruff format --check app/repositories/group_assistant_repository.py app/routers/groups.py app/schemas/assistant.py app/schemas/group.py app/services/group_service.py tests/integration/test_groups.py
uv --cache-dir /tmp/uv-cache run pytest tests/integration/test_groups.py
```

## 確認結果

- `ruff check`: All checks passed
- `ruff format --check`: 6 files already formatted
- `pytest tests/integration/test_groups.py`: 52 passed, 105 warnings in 0.92s

警告は既存の `app/schemas/auth.py` の Pydantic v2 deprecation と JWT テスト鍵長に関する警告であり、本Issueの実装に起因する失敗ではない。

## 未確認事項

- ブラウザによる手動操作確認は `naw-issue-loop` の方針により未実施
