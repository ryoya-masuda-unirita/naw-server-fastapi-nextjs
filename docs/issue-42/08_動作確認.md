# 08_動作確認

## 確認環境

- ローカルDB: `docker compose`起動済みの`naw-fastapi-postgres`（PostgreSQL、ポート5433）
- マイグレーション: `uv run alembic upgrade head`（008→010、`ai_models`・`assistant_category_mappings`テーブル作成）を実行
- サーバー: `uv run uvicorn app.main:app --port 8001`
- シードデータ: `backend/seed.sql`に、既存アシスタントとカテゴリの紐付け・AIモデル2件（`gpt-4o`・`claude-3-5-sonnet`）を追加し投入
- 認証: `create_access_token("admin", "test-tenant")`（テナント管理者）・`create_access_token("user01", "test-tenant")`（一般ユーザー）で発行したJWTを`Authorization: Bearer`ヘッダーに付与してcurlで直接呼び出し（issue-40と同じ方式）

## 確認手順・結果

| # | 操作 | 結果 |
|---|---|---|
| 1 | `GET /api/admin/assistants`（管理者） | 200、シード投入したアシスタント1件が、紐付けたカテゴリ・グループ・エンドポイントを含めて返る |
| 2 | `GET /api/admin/assistants`（一般ユーザー`user01`、管理者権限もグループ管理者権限もなし） | 403 |
| 3 | `GET /api/admin/assistants/AIModels`（管理者） | 200、シード投入したAIモデル2件が返る |
| 4 | `GET /api/admin/assistants/endpoints/SAAS_CHAT`（管理者） | 200、チャット系のテナントエンドポイントのみ返る |
| 5 | `POST /api/admin/assistants`（管理者、正しいリクエスト、`iconColor`未指定） | 200、ランダムなHEXカラーが`iconColor`に設定されて返る |
| 6 | `POST /api/admin/assistants`（`name`が空白のみ） | 422 |
| 7 | `PATCH /api/admin/assistants/{id}`（5で作成したID、`name`未送信・`description`のみ変更） | 200、`name`は元のまま維持され`description`のみ更新される |
| 8 | `DELETE /api/admin/assistants/{id}`（5で作成したID） | 204 |
| 9 | `PATCH /api/admin/assistants/{id}`（8で削除済みのID） | 404、`Assistant not found` |
| 10 | `GET /api/assistants`（一般ユーザー、issue-26実装分）| 200、紐付けたカテゴリが`category`/`categories`に実データとして返る（従来の常に空という挙動から変更） |

### code-review指摘対応後の追加確認

| # | 操作 | 結果 |
|---|---|---|
| 11 | AIModelをORM経由でINSERT（`alembic downgrade 008`→`upgrade head`でマイグレーション修正を反映後） | 成功（修正前は`asyncpg.exceptions.UndefinedObjectError: type "aimodelendpointtype" does not exist`で失敗） |
| 12 | `GET /api/admin/assistants?sort=assistantType,asc`（フロントエンドが実際に送信するソートキー） | 200、`type`昇順で正しくソートされる（修正前は無言で`updatedAt`ソートにフォールバック） |
| 13 | `PATCH /api/admin/assistants/{id}`（`endpoints`に同一テナントエンドポイントIDを重複指定） | 400（修正前は複合PK違反で未処理の500） |
| 14 | `PATCH /api/admin/assistants/{id}`（`name`を空文字で送信） | 200、既存の名前が維持される（修正前は422） |

## テスト実行結果

- `uv run pytest -q`: 246 passed（code-review指摘対応で追加した4件を含む。既存219件は非破壊）
- `uv run ruff check .`: All checks passed
- `uv run ruff format .`: 実行し整形
- `uv run mypy app/`: 既存から38件増加（89→127件）。いずれも本プロジェクト全体に既存する`sa.Column`型付け起因の既知の誤検知パターンと同種で、CIも`ruff`のみを実行しmypyはゲートしていないため許容する

## 未確認事項

- `frontend-angular`からの画面経由での動作確認: ログイン画面がテナントサブドメイン経由の`X-Tenant-ID`解決を前提としており、`localhost:4201`への直接アクセスでは`POST /auth/login`が422（`X-Tenant-ID`ヘッダー欠如）になる。これは本チケット非対象の既存のログイン導線の制約であり、issue-40同様バックエンドAPIをcurlで直接確認する方式とした
- room（チャット本体）が未移植のため、デフォルトアシスタント使用中の削除時409判定は未確認（本チケットスコープ外）
