# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] マイグレーション`009_add_ai_models.py`・`010_add_assistant_category_mappings.py`を追加
- [x] `app/models/ai_model.py`を新規作成
- [x] `app/models/assistant.py`に`AssistantCategoryMapping`を追加
- [x] `app/core/security.py`に`require_admin_or_group_admin`を追加
- [x] `app/repositories/assistant_category_mapping_repository.py`を新規作成
- [x] `app/repositories/ai_model_repository.py`を新規作成
- [x] `app/repositories/assistant_repository.py`に作成・更新・削除・`find_page`を追加
- [x] `app/repositories/group_assistant_repository.py`に`replace_groups_for_assistant`を追加
- [x] `app/repositories/assistant_endpoint_repository.py`に`find_by_assistant_id_and_tenant_id`・`replace_endpoints_for_assistant`を追加
- [x] `app/repositories/tenant_endpoint_repository.py`に`find_by_ids_and_tenant_id`を追加
- [x] `app/repositories/assistant_category_repository.py`に`find_by_tenant_id_and_ids`を追加
- [x] `app/schemas/assistant.py`にリクエスト/レスポンススキーマを追加
- [x] `app/services/assistant_service.py`に作成・更新・削除・一覧・エンドポイント一覧・AIモデル一覧を追加
- [x] 一般ユーザー向け`get_assistants`のカテゴリ実データ化
- [x] `app/routers/assistants.py`に管理者向けルーターを追加
- [x] `app/main.py`にルーター登録を追加
- [x] `backend/app/models/assistant.py`の未移植コメントを更新
- [x] `seed.sql`にAIモデル・アシスタントカテゴリ紐付け等の動作確認用データを追加

## テスト

- [x] `tests/integration/test_assistants.py`に管理者向けCRUD・一覧・エンドポイント一覧・AIモデル一覧のテストを追加
  - 05_テスト詳細設計.mdでは`tests/unit/test_assistants_admin_router.py`・`tests/integration/test_assistants_admin.py`を新設する想定だったが、
    このリポジトリの実際の慣習（ルーターテストは`tests/integration/`に集約、`tests/unit/`はサービス層のモックテスト）に合わせ、
    既存の`tests/integration/test_assistants.py`に一本化した
- [x] `tests/integration/test_assistants.py`の既存ケース（`GET /api/assistants`）にカテゴリ実データ化のテストケースを追加
- [x] `tests/integration/conftest.py`の共有クリーンアップ対象テーブルに`ai_models`を追加（テナントに属さないグローバルテーブルのため）
- [x] `pytest`・`ruff`をすべて通す（`mypy`は既存から同種の誤検知が37件増加するのみでCI非対象のため許容）

## 動作確認

- [x] curl経由でアシスタント作成・更新・削除・一覧・権限分岐・エンドポイント一覧・AIモデル一覧を確認（`frontend-angular`のログイン導線がテナントサブドメイン前提のため、issue-40同様curl直接確認方式とした）
- [x] 動作確認結果を`08_動作確認.md`に記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実施し、`code-review.md` を作成
- [ ] 指摘修正 → 再テスト → 再動作確認 → PR 修正
