# 08_動作確認

## 確認環境

本Issueでは `naw-issue-loop` 全自動フローの上書きルールにより、ブラウザでの実操作確認は行わず、自動テスト（pytest）の実行結果のみで代替する。

## 確認手順

```bash
cd backend
pytest
```

## 確認結果

`backend/tests/unit` を全件実行し、本Issueで追加・変更した以下を含めすべて成功した。

```
$ uv run python -m pytest tests/unit -q
184 passed, 17 warnings in 10.06s
```

- 追加した回帰テスト（`test_response_format_schema.py`・`test_llm_client.py`・
  `test_llm_chat_service.py`・`test_message_service.py`の新規テストメソッド）を含め、
  既存テストも含めて全件成功（デグレなし）
- 併せて `ruff check`・`mypy` を変更ファイルに対して実行し、いずれもエラーなしを確認した
  （`mypy`は `app/repositories/` 配下に本Issue対応前から存在する既存の型エラーがあるが、
  今回変更したファイル（`schemas/response_format.py`・`schemas/llm.py`・
  `schemas/message.py`・`core/llm_client.py`・`services/llm_chat_service.py`・
  `services/message_service.py`）には該当しないことを確認済み）

## 未確認事項

- ブラウザ・`frontend-angular`（http://localhost:4201）経由での実際のUI操作は未実施
- Azure OpenAI実サービスに対する疎通確認（`response_format`指定時に実際にJSON形式の応答が返るか）は未実施。テストはすべてAzure SDK呼び出しをモックした単体テストのみ
