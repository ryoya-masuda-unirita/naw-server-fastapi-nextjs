# #100 issue-100 response_format(構造化出力)対応

## 概要

LLM Chat/Messageで、response_format（構造化出力）指定に対応する。

## 説明

issue-88・issue-93の両方でresponse_format対応は意図的にスコープ外とされていた。本Issueでその対応を行う。

- 移植元（バックエンド）: `~/Documents/naw-server/src/main/java/com/example/naw/service` 配下のresponse_format関連実装
- 移植先: `backend/app/services/message_service.py`, `backend/app/services/llm_chat_service.py`
- 関連Issue: #88, #93（スコープ外節を参照）
- 参照調査: `docs/調査結果/20260711.md`（4章 項目5）

## その他

- response_format指定時の構造化出力（JSON等）でのLLM呼び出しに対応する。
