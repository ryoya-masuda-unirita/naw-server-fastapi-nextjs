# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装（backend、先行実施済み）

- [x] `backend/app/services/auth_service.py`にパスワード有効期限切れ（`reason=EXPIRED`）判定を追加（移植漏れの修正）
- [x] `backend/tests/unit/test_auth_service.py`にEXPIRED判定・優先順位のテストを追加、既存テストの`expired_at`モック不備を修正
- [x] `backend/seed.sql`に`first-login-user`・`expired-password-user`を追加

## 実装（frontend-angular / Playwright基盤）

- [ ] `frontend-angular`に`@playwright/test`を追加（`npm install -D @playwright/test` + `npx playwright install chromium`）
- [ ] `frontend-angular/playwright.config.ts`を作成
- [ ] `frontend-angular/e2e/fixtures/auth.fixture.ts`を作成
- [ ] `frontend-angular/e2e/login.spec.ts`を作成
- [ ] `frontend-angular/e2e/README.md`を作成
- [ ] `frontend-angular/package.json`に`test:e2e`スクリプトを追加
- [ ] `frontend-angular/.gitignore`に`playwright-report/`・`test-results/`を追加

## テスト

- [ ] `backend`: `pytest tests/unit` 全件成功を確認（実施済み、再確認）
- [ ] `frontend-angular`: `npm test`（vitest、既存単体テスト）全件成功を確認
- [ ] `frontend-angular`: `npx playwright test`（新規E2Eテスト）全件成功を確認

## 動作確認

- [ ] `docker compose up -d` → `npm run start:local`でローカル環境を起動し、`npx playwright test`が実際に全自動実行できることを確認
- [ ] `npx playwright show-report`でレポートが生成されることを確認

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
- [ ] 指摘（🔴致命的は必須、🟡🔵はAI判断）に対応
