# 06_タスクリスト

## 実装

- [ ] `backend/app/models/group.py` に `Group`・`GroupUser` モデルを追加する
- [ ] `backend/alembic/versions/003_add_groups.py` を作成し `groups`・`groups_users` テーブルを追加する
- [ ] `backend/app/repositories/user_repository.py` に `find_by_id_and_tenant_id` を追加する
- [ ] `backend/app/repositories/group_repository.py` を新規作成する
- [ ] `backend/app/repositories/group_user_repository.py` を新規作成する（`find_by_group` のN+1回避方針を確定してから実装する）
- [ ] `backend/app/schemas/group.py` を新規作成する
- [ ] `backend/app/services/group_service.py` を新規作成する
- [ ] `backend/app/routers/groups.py` を新規作成する（`GET .../users` 系エンドポイントに `response_model_exclude_none=True` を付与する）
- [ ] `backend/app/main.py` に新規ルーターを登録する
- [ ] `backend/seed.sql` に一般ユーザー2名（`user01`・`user02`）を追加する（実際のbcryptハッシュを生成し直す）

## テスト

- [ ] `tests/integration/conftest.py` のテーブルクリア対象に `groups_users`・`groups` を追加する
- [ ] `tests/integration/test_groups.py` を新規作成する（`GET /api/groups`・`/api/admin/groups`・`/api/admin/all-groups`・グループCRUD・所属ユーザー管理の一連のテスト）
- [ ] `tests/unit/test_group_service.py` を新規作成する（権限判定ロジックのテスト）
- [ ] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## マイグレーション適用・動作確認

- [ ] `alembic upgrade head` でマイグレーションを適用する
- [ ] `backend/seed.sql` を再投入する
- [ ] API直叩き（curl等）で以下を確認する
  - [ ] `POST /api/admin/groups` でグループ作成
  - [ ] `POST /api/admin/groups/{id}/users` でユーザー追加
  - [ ] `GET /api/admin/groups`・`GET /api/admin/groups/{id}/users` のレスポンス形式確認
- [ ] `frontend-angular`（`http://localhost:4201`）経由で管理画面のグループ一覧・詳細・所属ユーザー管理画面が表示・操作できることをPlaywrightで確認する

## PR作成・レビュー

- [ ] PR作成（`--base develop`）
- [ ] `/code-review` 実行
- [ ] `docs/issue-24/code-review.md` 作成
- [ ] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- `frontend-angular` の管理画面からグループのCRUD・所属ユーザー管理が一通り操作できることを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
