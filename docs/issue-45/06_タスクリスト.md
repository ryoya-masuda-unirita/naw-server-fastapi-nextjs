# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] Alembicマイグレーション `011_add_rooms_and_room_pins.py` を追加
- [x] `app/models/room.py` に `Room` / `RoomPin` / `RoomRating` を追加
- [x] `app/schemas/room.py` を新規作成し、CRUD・一覧レスポンスを定義
- [x] `app/repositories/room_repository.py` を新規作成
- [x] `app/repositories/room_pin_repository.py` を新規作成
- [x] `app/services/room_service.py` を新規作成
- [x] `app/routers/rooms.py` を新規作成
- [x] `app/main.py` に rooms ルーター登録を追加
- [x] `app/services/assistant_service.py` に参照中アシスタント削除時の400ハンドリングを追加

## テスト

- [x] `backend/tests/integration/test_rooms.py` を追加
- [x] `backend/tests/integration/test_assistants.py` にアシスタント削除回帰テストを追加
- [x] 必要に応じて `conftest.py` のfixture / cleanup を拡張
- [x] `pytest` を実行して追加・既存テストが通ることを確認
- [x] `ruff check app tests` を実行して静的チェックを確認

## 動作確認

- [x] ルーム作成、一覧、更新、削除、固定、固定解除を確認
- [x] 参照中アシスタント削除時に400となることを確認
- [x] 結果を `08_動作確認.md` に記録

## PR・レビュー

- [ ] 実装方式（Human in the Loop / 全自動）の承認を得る
- [ ] 必要なコミットを作成する
- [ ] PR を作成する
- [ ] `/code-review` と指摘対応を行う
