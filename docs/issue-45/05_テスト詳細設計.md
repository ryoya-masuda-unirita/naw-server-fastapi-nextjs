# 05_テスト詳細設計

## `backend/tests/integration/test_rooms.py`（新規）

```
class TestGetRooms:
    test_get_rooms_returns_only_owned_rooms
    test_get_rooms_orders_pinned_first_then_updated_at_desc
    test_get_rooms_filters_by_name_case_insensitive
    test_get_rooms_returns_422_when_page_is_negative
    test_get_rooms_returns_422_when_size_is_out_of_range
    test_get_rooms_returns_403_when_tenant_header_mismatches_token

class TestGetRoom:
    test_get_room_returns_owned_room
    test_get_room_returns_404_for_other_users_room
    test_get_room_returns_404_for_unknown_room

class TestCreateRoom:
    test_create_room_succeeds
    test_create_room_truncates_name_over_255_chars
    test_create_room_returns_400_for_invalid_assistant_id
    test_create_room_returns_401_without_authentication

class TestUpdateRoom:
    test_update_room_succeeds
    test_update_room_truncates_name_over_255_chars
    test_update_room_returns_404_for_other_users_room

class TestDeleteRoom:
    test_delete_room_succeeds
    test_delete_room_deletes_related_room_pin_by_cascade
    test_delete_room_returns_404_for_other_users_room

class TestPinRoom:
    test_pin_room_succeeds
    test_pin_room_is_idempotent
    test_pin_room_returns_404_for_other_users_room
    test_pin_room_returns_404_for_unknown_room

class TestUnpinRoom:
    test_unpin_room_succeeds
    test_unpin_room_returns_404_when_room_is_not_pinned
    test_unpin_room_returns_404_for_unknown_room
```

## `backend/tests/integration/test_assistants.py`（既存拡張）

```
class TestDeleteAssistant:
    test_delete_assistant_returns_400_when_used_as_default_assistant_of_room
    test_delete_assistant_succeeds_when_no_room_references_it
```

`rooms.default_assistant_id` の FK 制約追加に伴う既存API影響をここで回帰確認する。

## `backend/tests/integration/conftest.py` または `backend/tests/conftest.py`（必要に応じて拡張）

```
fixture tenant_for_rooms
fixture room_owner_user
fixture another_user_same_tenant
fixture assistant_for_room
fixture room
fixture pinned_room
fixture auth_headers_for_room_owner
fixture auth_headers_for_other_user
```

- 既存 fixture を流用できる場合は新規作成せず、`test_assistants.py` などのパターンに合わせて最小限の追加に留める
- `Room` / `RoomPin` の cleanup が必要なら共通 cleanup 対象へ追加する

## `backend/tests/unit/test_room_pin_domain.py`（任意）

固定・固定解除の振る舞いを helper に切り出した場合のみ追加する。

```
class TestPinDecision:
    test_pin_returns_success_when_room_exists_and_not_pinned
    test_pin_returns_success_when_already_pinned
    test_pin_returns_room_not_found_when_room_is_missing

class TestUnpinDecision:
    test_unpin_returns_success_when_pinned
    test_unpin_returns_room_not_found_when_room_is_missing
    test_unpin_returns_not_pinned_when_pin_does_not_exist
```

## 確認対象データ

- 同一テナント内で「所有者本人のルーム」「別ユーザーのルーム」「固定済みルーム」「未固定ルーム」を用意する
- `assistantId` 検証用に「同一テナントの有効アシスタント」「別テナントのアシスタント」「存在しないID」を用意する
- アシスタント削除回帰テスト用に、`rooms.default_assistant_id` が対象を参照するルームを1件作成する
