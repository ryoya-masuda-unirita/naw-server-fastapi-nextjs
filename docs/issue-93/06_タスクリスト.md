# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成（全自動モード、SAAS_CHATのみへスコープ縮小）
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `app/schemas/message.py` に `MessageContentHistoryTurn` / `MessageContentCreateRequest` を追加
- [x] `app/repositories/message_content_repository.py` に `save` を追加
- [x] `app/repositories/assistant_endpoint_repository.py` に `find_chat_endpoint` を追加
- [x] `app/services/message_service.py` に `stream_message_content` を追加
- [x] `app/routers/messages.py` に `POST /content` を追加

## テスト

- [x] `tests/unit/test_message_service.py` に分岐網羅テストを追加
- [x] `tests/integration/test_messages.py` にSSE正常系・404系の統合テストを追加
- [x] `uv run pytest -q` が全件成功することを確認

## 動作確認

- [x] `08_動作確認.md` に自動テスト実行結果を記録（ブラウザ操作は行わない旨を明記）

## PR・レビュー

- [ ] PR を作成（`Closes #93`、Issue #88(PR #91)マージ済み・依存関係を明記）
- [ ] `/code-review` を実行し `code-review.md` を作成、🔴致命的指摘があれば修正
