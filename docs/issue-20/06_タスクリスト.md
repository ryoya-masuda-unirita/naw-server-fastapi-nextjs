# 06_タスクリスト

## 実装

- [x] `backend/app/core/config.py` に `Settings.cookie_secure` を追加する
- [x] `backend/.env.example` に `COOKIE_SECURE=false` を追加する
- [x] `backend/app/core/security.py` にCookie名定数・`set_access_token_cookie`・`clear_access_token_cookie` を追加する
- [x] `backend/app/core/security.py` の `security = HTTPBearer()` を `auto_error=False` に変更する
- [x] `backend/app/core/security.py` の `get_current_user` をCookie優先＋Authorizationフォールバック方式に変更する
- [x] `backend/app/routers/auth.py` の `login` でCookieを設定するよう変更する
- [x] `backend/app/routers/auth.py` の `logout` でCookieを削除するよう変更する
- [x] `backend/app/routers/auth.py` の `reset_password` でCookieを設定するよう変更する
- [x] `backend/app/routers/auth.py` の `get_auth` でCookieを再設定するよう変更する

## テスト

- [x] `tests/unit/test_config.py` に `Settings.cookie_secure` のテストを追加する
- [x] `tests/unit/test_security.py` に `set_access_token_cookie` / `clear_access_token_cookie` のテストを追加する
- [x] `tests/integration/test_auth.py` にログイン時Cookie発行・ログアウト時Cookie削除・Cookie認証・未認証401のテストを追加する
- [x] `tests/integration/test_users.py` にCookieのみでの認証テストを追加する
- [x] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## 動作確認

- [x] `frontend-angular`（`http://localhost:4201`）経由でログイン → API呼び出し → ページリロードの一連の流れをPlaywrightで確認する
- [x] ログイン後にブラウザの開発者ツールで `access_token` CookieがHttpOnlyで発行されていることを確認する
- [x] ログアウト後に `access_token` Cookieが削除されていることを確認する

## PR作成・レビュー

- [x] PR作成（`--base develop`）
- [x] `/code-review` 実行
- [x] `docs/issue-20/code-review.md` 作成
- [x] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正（🔴致命的指摘なし、🟡指摘1件は対応しない判断のため再テスト・再動作確認不要）

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- `frontend-angular` 経由でログイン後のAPI呼び出し・リロードが401にならないことを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
