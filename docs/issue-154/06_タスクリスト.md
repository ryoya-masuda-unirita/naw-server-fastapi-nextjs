# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `routers/users.py` の `size` を `le=1000` に変更
- [x] `routers/assistants.py` の `size` を `le=1000` に変更
- [x] `routers/prompt_templates.py` の `size`（2箇所）を `le=1000` に変更
- [x] `routers/indexes.py` の `size` を `le=1000` に変更

## テスト

- [x] `test_users.py` に `size=1000`→200・`size=1001`→422 のテストを追加
- [x] `uv run pytest` で全テスト成功を確認（761 passed）
- [x] `uv run mypy app` で型エラーがないことを確認
- [x] `uv run ruff check app` でlintエラーがないことを確認

## 動作確認

- [x] 以前422だった `size=1000` の一覧APIが200になることを確認
- [x] 動作確認を `08_動作確認.md` に記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
