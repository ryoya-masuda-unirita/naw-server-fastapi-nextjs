# 06_タスクリスト

全自動モードで進行。完了ごとにチェックする。

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `app/core/library_stream_router.py`（`LibraryStreamRouter`・マーカー定数・`LIBRARY_USER_INSTRUCTION_PREFIX`）を作成
- [x] `app/models/system_prompt_template.py`（`SystemPromptTemplate`モデル）を作成
- [x] `app/repositories/system_prompt_template_repository.py`を作成
- [x] `alembic/versions/022_add_system_prompt_templates.py`（テーブル作成 + LIBRARYプロンプトのシード投入）を作成
- [x] `app/schemas/llm.py`に`createLibrary`フィールドを追加
- [x] `app/schemas/message.py`に`isCreateLibrary`フィールドを追加
- [x] `app/services/llm_chat_service.py`に`createLibrary`対応を実装
- [x] `app/services/message_service.py`に`isCreateLibrary`対応を実装

## テスト

- [x] `tests/unit/test_library_stream_router.py`を作成
- [x] `tests/unit/test_llm_chat_service.py`にライブラリ生成モードのテストを追加
- [x] `tests/unit/test_message_service.py`にライブラリ生成モードのテストを追加
- [x] `tests/integration/test_system_prompt_templates.py`を作成
- [x] `pytest`を実行し全テストが通ることを確認（653 passed）

## 動作確認

- [x] `08_動作確認.md`に自動テスト結果を記録（ブラウザでの実操作確認は行わない）

## PR・レビュー

- [x] PR を作成（#108）
- [x] `/code-review`を実行し`code-review.md`を作成
- [x] 🔴致命的指摘があれば修正（🔴致命的指摘はなし。🟡注意1件は対応済み、🔵提案1件は対応見送り）
