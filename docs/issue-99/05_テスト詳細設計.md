# 05_テスト詳細設計

## 対象ファイル

- `backend/tests/unit/test_library_stream_router.py`（新規）
  - `TestFeed`: `04_テスト設計.md`のNo.1・2・5に対応
  - `TestFinal`: No.3・4・6に対応
- `backend/tests/unit/test_llm_chat_service.py`（既存へ追加）
  - `TestStreamChat`に、No.7・8・9に対応するテストメソッドを追加
- `backend/tests/unit/test_message_service.py`（既存へ追加）
  - `stream_message_content`関連のテストクラスに、No.10・11・12に対応するテストメソッドを追加
- `backend/tests/integration/`（新規）
  - `test_system_prompt_templates.py`: No.13に対応（`alembic upgrade head`適用後のシード確認）

## テストデータ・モック方針

- `LibraryStreamRouter`は外部依存なしのため、モック不要で直接インスタンス化してテストする。
- LLM Chat API・メッセージ送信APIのテストは、既存テスト同様`AzureLlmChatClient.stream_chat`・各Repositoryをモックし、`SystemPromptTemplateRepository.find_by_type`もモックして固定プロンプト文字列を返すようにする。
- `LibraryRepository.save`はモックし、呼び出し引数（title・content・tenant_id・message_id・user_id）を検証する。
