# #99 issue-99 ライブラリ生成(createLibrary, SSEイベント)対応

## 概要

Message/LLM Chat双方から呼び出される、LLMによるライブラリ新規生成（`createLibrary`）機能に対応する。生成過程はSSEイベント（`library_title_delta`/`library_content_delta`）でストリーミング配信する。

## 説明

issue-88・issue-93の両方でライブラリ生成対応は意図的にスコープ外とされていた。issue-65（Library本体のCRUD）では生成機能自体は未着手のまま。

- 移植元（バックエンド）: `~/Documents/naw-server/src/main/java/com/example/naw/service` 配下のライブラリ生成関連実装
- 移植先: `backend/app/services/message_service.py`, `backend/app/services/llm_chat_service.py`, `backend/app/routers/libraries.py`
- 関連Issue: #65, #88, #93（スコープ外節を参照）

### スコープ（想定）

- `createLibrary`呼び出し時のライブラリ新規生成
- `library_title_delta`/`library_content_delta` SSEイベントの配信

## その他

- 元チケット（NAW番号）の記載なし。
- 移植元の詳細調査結果は`03_詳細設計.md`に記載する。
