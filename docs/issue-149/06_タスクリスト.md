# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/pyproject.toml`に`anthropic`を追加
- [x] `backend/app/models/tenant_endpoint.py`の`EndpointType`に`BEDROCK_CHAT`を追加
- [x] `backend/app/models/ai_model.py`の`AIModelEndpointType`に`BEDROCK_CHAT`を追加
- [x] `backend/alembic/versions/023_add_bedrock_chat_endpoint_type.py`を新規作成
      （`endpointtype`・`aimodelendpointtype`enumへの値追加）
- [x] `backend/app/repositories/ai_model_repository.py`に`find_by_name`を追加
- [x] `backend/app/core/llm_client.py`に`BedrockLlmChatClient`を追加
- [x] `backend/app/services/llm_chat_service.py`の呼び出し分岐をAIモデルのエンドポイント
      種別に応じて一般化し、`_persist_token_usage`の`endpoint_type`固定値を修正
- [x] `backend/seed.sql`にBedrock用の`tenant_endpoints`行を追加

## テスト

- [x] `backend/tests/integration/test_ai_model_repository.py`を新規作成し`find_by_name`のテストを追加
- [x] `backend/tests/unit/test_llm_client.py`に`BedrockLlmChatClient`のテストを追加
- [x] `backend/tests/unit/test_llm_chat_service.py`のモック対象を`find_by_name`へ置換し、
      `_ai_model`・`_tenant_endpoint`ヘルパーにエンドポイント種別引数を追加、Bedrock分岐の
      テストケースを追加
- [x] `uv run pytest` で全テスト成功を確認（770 passed）
- [x] `uv run mypy app` で型エラーがないことを確認（変更・新規ファイル自体にエラーなし）
- [x] `uv run ruff check app` でlintエラーがないことを確認

## 動作確認

- [ ] 実際のAWS Bedrock API key・Bedrock Mantleエンドポイントを用意できるか確認する
      （用意できない場合、この段階での実機確認は不可否併記する）
- [ ] ローカルでbackendを起動し、Bedrock用エンドポイントを登録したテナントでチャットAPIを
      呼び出し、Claude（Bedrock経由）から応答が返ることを確認する
- [ ] `token_usages`にBedrock分の利用量が記録されることを確認する
- [ ] 動作確認を `08_動作確認.md` に記録

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
