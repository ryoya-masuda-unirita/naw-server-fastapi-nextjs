# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/app/schemas/feedback.py` にリクエスト・レスポンススキーマを追加する
- [x] `backend/app/repositories/message_feedback_repository.py` に `find_feedback_users`（CTE集計クエリ）を追加する
- [x] `backend/app/services/feedback_service.py` を追加する
- [x] `backend/app/routers/feedback.py` を追加し `backend/app/main.py` に登録する

## テスト

- [x] `backend/tests/integration/test_feedback.py` を追加する（05のテストクラス構成に沿って実装）
- [x] `pytest` を実行し全テストが通ることを確認する
- [ ] `mypy app/`・`ruff check app/` を実行し警告がないことを確認する
  - `ruff check app tests` は通過
  - `mypy app` は既存コード由来のエラーが多数あり未解消

## 動作確認

- [x] ローカル環境（`uvicorn` + Docker PostgreSQL）で動作確認する
- [x] `curl` で各種絞り込み・ソート・ページング・権限を実際に呼び出して動作確認する
- [x] `08_動作確認.md` に確認結果を記録する

## PR・レビュー

- [x] PR を作成する（`Closes #52` を本文に含める）
- [x] `prepare_code_review.sh` でベース差分を確定し、Codex 自身でレビューする
- [x] `code-review.md` を作成する
- [x] 指摘（🔴 致命的は必須）を修正し、再テスト・再動作確認する
  - 指摘なしだったため、追加修正は発生しなかった
