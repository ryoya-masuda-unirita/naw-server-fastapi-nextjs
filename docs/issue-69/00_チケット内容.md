# GroupController グループ×プロンプトテンプレート紐付け管理APIの移植

## 概要

`~/Documents/naw-server`（Spring Boot）の `GroupController` にある「グループ×プロンプトテンプレート紐付け管理」APIをFastAPIへ移植する。

## 説明

対象エンドポイント:

| メソッド | パス | 概要 |
|---|---|---|
| POST | `/api/admin/groups/{groupId}/prompt-templates` | 指定プロンプトテンプレートをグループに追加 |
| DELETE | `/api/admin/groups/{groupId}/prompt-templates/{templateId}` | 指定プロンプトテンプレートをグループから除外 |
| GET | `/api/admin/groups/{groupId}/prompt-templates` | グループに所属するプロンプトテンプレート一覧取得（`search`フィルタ、ページネーション） |

`GroupController`にはこの他にアシスタント紐付け管理（3エンドポイント）もあるが、フィルタ・ソートロジックがより複雑なため別Issueとして切り出されている。本Issueはプロンプトテンプレート紐付けのみを対象とする。

参照実装:
- Controller: `~/Documents/naw-server/src/main/java/com/example/naw/api/GroupController.java`（L222-255, L284-294）
- Service: `~/Documents/naw-server/src/main/java/com/example/naw/service/GroupService.java`（`addGroupPromptTemplates`, `removeGroupPromptTemplate`, `findGroupPromptTemplates`）
- Repository/Entity: `GroupPromptTemplateRepository`, `GroupPromptTemplate`, `GroupPromptTemplateId`, `GroupPromptTemplateSpecification`
- Request/Response: `GroupPromptTemplatesAddRequest`, `PromptTemplateResponse`（`addedAt`フィールドあり）

## その他

- 本対応はNAWチケット参照なし（issue-69として扱う）
- 全自動モードで対応（HITL承認省略、ブラウザ動作確認は自動テストで代替）
