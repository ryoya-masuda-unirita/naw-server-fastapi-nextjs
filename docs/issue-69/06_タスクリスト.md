# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `schemas/prompt_template.py` に `addedAt` フィールドを追加
- [x] `schemas/group.py` に `GroupPromptTemplatesAddRequest` を追加
- [x] `repositories/prompt_template_repository.py` に `find_by_ids_and_tenant_id` を追加
- [x] `repositories/group_prompt_template_repository.py` に `find_one`/`find_page_by_group`/`add`/`remove` を追加
- [x] `services/group_service.py` に `add_group_prompt_templates`/`remove_group_prompt_template`/`list_group_prompt_templates` を追加
- [x] `routers/groups.py` に3エンドポイントを追加

## テスト

- [x] `tests/integration/test_groups.py` に `TestGroupPromptTemplates` を追加
- [x] `pytest` 全体実行で既存テストにリグレッションがないことを確認

## 動作確認

- [x] 自動テストのみで代替（ブラウザ操作は行わない旨を `08_動作確認.md` に明記）

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
