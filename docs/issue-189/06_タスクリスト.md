# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `infra/tfstate_backend.tf` を新規作成（tfstate用S3バケット）
- [x] `infra/github_oidc.tf` を新規作成（OIDCプロバイダ・IAMロール・ポリシー）
- [x] `infra/main.tf` にリモートバックエンド設定を追加（コメントで移行手順を明記）
- [x] `infra/ecs.tf` に `lifecycle { ignore_changes }` を追加（CIのデプロイ結果をterraform applyが巻き戻さないようにする対応。03の詳細設計には無かったが実装中に必要と判明）
- [x] `.github/workflows/deploy-frontend-angular.yml` を `workflow_call` トリガーで作成
- [x] `.github/workflows/deploy-backend.yml` を `workflow_call` トリガーで作成
- [x] `.github/workflows/deploy-infra.yml` を `workflow_call` トリガーで作成
- [x] `.github/workflows/deploy.yml`（司令塔）を新規作成。Reusable Workflow + needsで順序制御を実現（03の詳細設計を更新済み）
- [x] `frontend`ジョブも`infra`ジョブの完了待ちに変更（frontendが使うS3/CloudFrontがinfra変更で作り直される可能性への対応）

## テスト

- [x] `terraform fmt -check -diff` を実行
- [x] `terraform validate` を実行
- [x] OIDCロール・IAMポリシーのスコープを目視確認（`terraform apply`権限不足を発見しPowerUserAccess+IAMFullAccessを追加）
- [x] ワークフローYAML構文・パスフィルタを確認
- [x] `terraform plan`（OIDC/tfstateバケット関連を`-target`）を実行（実行前にユーザーへ確認）

## 動作確認

- [x] `terraform plan` の出力内容を `08_動作確認.md` に記録

## PR・レビュー

- [ ] PR を作成
- [x] `/code-review` を実行し `code-review.md` を作成
