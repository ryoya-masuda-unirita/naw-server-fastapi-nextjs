# 05_テスト詳細設計

## 実行コマンドと対象

| No | 対応する04のNo | コマンド | 実行ディレクトリ |
|---|---|---|---|
| 1 | 1 | `terraform validate` | `infra/` |
| 2 | 2 | `terraform fmt -check -diff` | `infra/` |
| 3 | 3 | `terraform plan -target=aws_iam_openid_connect_provider.github -target=aws_iam_role.github_actions -target=aws_iam_role_policy.github_actions -target=aws_s3_bucket.tfstate` | `infra/` |
| 4, 5 | 4, 5 | `infra/github_oidc.tf` を目視確認 | - |
| 6 | 6 | `actionlint`（未インストールの場合は`python3 -c "import yaml; yaml.safe_load(open('...'))"`等でYAML構文のみ確認） | リポジトリルート |
| 7 | 7 | 各`.github/workflows/*.yml`の`on.push.paths`を目視確認 | - |

## 実行者

- No.1, 2, 4, 5, 6, 7: Claudeが実行・確認する
- No.3: `terraform plan`はAWS実環境と通信するため、実行前にユーザーへ確認する（読み取り専用操作）
