# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `infra/acm.tf` を削除する
- [x] `infra/cloudfront.tf`: `aliases`削除、`viewer_certificate`をデフォルト証明書に変更、Route53 Aレコード（root/wildcard）を削除する
- [x] `infra/main.tf`: `data "aws_route53_zone" "main"`と`us_east_1`プロバイダエイリアスを削除する
- [x] `infra/variables.tf`: `variable "domain_name"`を削除する
- [x] `infra/terraform.tfvars` / `infra/terraform.tfvars.example`: `domain_name`指定行を削除する
- [x] `infra/ecs.tf`: `CORS_ALLOWED_ORIGIN_REGEX`を空文字化、`CORS_ALLOWED_ORIGINS`をCloudFrontデフォルトドメイン参照に変更する
- [x] `infra/README.md`: ドメイン運用・デプロイ手順・destroy時の注意を更新する

## テスト

- [x] `terraform fmt -check -recursive` を実行する（差分なし）
- [x] `terraform validate` を`infra/`で実行する（Success）
- [x] `terraform plan` を`infra/`で実行する → 実行不可（詳細は`08_動作確認.md`参照）

## 動作確認

- [x] 実行結果を`08_動作確認.md`に記録する
- [ ] 実際のAWS環境への反映（`terraform apply`）はユーザーが実施し、結果を共有してもらう

## PR・レビュー

- [ ] PR を作成する
- [ ] `/code-review` を実行する
- [ ] `code-review.md` を作成する
