# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## フェーズ0: 準備（コストなし・低コスト）

- [ ] 作業用IAMユーザー（`AdministratorAccess`）を作成し、`~/.aws/config`にプロファイル登録（root運用の廃止）
- [ ] IAMロール（ECSタスク実行ロール・ECSコンテナインスタンス用ロール）を作成
- [ ] 踏み台SSH用キーペアを作成
- [ ] VPC・パブリックサブネット2つ・IGW・ルートテーブルを作成
- [ ] セキュリティグループ5つ（`sg-bastion`/`sg-alb`/`sg-ecs`/`sg-rds`/`sg-elasticache`）を作成
- [ ] ECRリポジトリを作成し、`backend/Dockerfile`でビルドしたイメージをpush
- [ ] `frontend-angular`をビルドし、S3バケットへアップロード
- [ ] CloudFrontディストリビューション（Price Class 100、OAC設定）を作成し、デプロイ完了を待つ

## フェーズ1: DB・キャッシュ・踏み台

- [ ] RDS用DBサブネットグループ・RDSインスタンス（`db.t4g.micro`、Single-AZ、パブリックアクセス無効）を作成
- [ ] ElastiCache用サブネットグループ・Redisクラスタ（`cache.t4g.micro`）を作成
- [ ] 踏み台EC2（`t4g.nano`）を起動
- [ ] 踏み台経由でRDSへ接続し、Alembicマイグレーション・`backend/seed.sql`投入を実行

## フェーズ2: ECS（backend）

- [ ] ECSクラスタ（EC2起動タイプ）・ECSホストEC2（`t4g.nano`）を起動
- [ ] ECSタスク定義（DATABASE_URL/REDIS_URL/COOKIE_SECURE/COOKIE_SAME_SITE等の環境変数）を登録
- [ ] ECSサービスを作成し、ECSホスト単体での疎通（`curl http://<ECSホストIP>:8000/...`）を確認

## フェーズ3: ALB + HTTPS化

- [ ] ALB・ターゲットグループ・HTTPリスナーを作成
- [ ] ALBの現在のパブリックIPを確認し、`<ip>.sslip.io`の名前解決を確認
- [ ] certbotでHTTP-01検証を実行し証明書を取得
- [ ] 取得した証明書をACMへインポート
- [ ] ALBにHTTPSリスナーを追加し、証明書をアタッチ
- [ ] `https://<ip>.sslip.io/`への疎通を確認

## フェーズ4: 動作確認

- [ ] `04_テスト設計.md`のNo.1〜9を実施し、`08_動作確認.md`に結果を記録
- [ ] CloudFront（frontend-angular）からALB（backend）へのクロスオリジンセッションCookie認証が機能することを確認

## フェーズ5: 全リソース削除

- [ ] `03_詳細設計.md`のフェーズ5の順序に従い、ALB→ECS→踏み台→ElastiCache→RDS→CloudFront→S3→ECR→SG→VPC関連の順に削除
- [ ] `aws resourcegroupstaggingapi get-resources --tag-filters Key=Project,Values=issue-158`で0件を確認
- [ ] 翌日以降にAWS Billingで新規課金が発生していないことを確認し、`08_動作確認.md`に記録

## PR・レビュー

- [ ] PR を作成（コード変更がない場合はドキュメントのみのPRとする）
- [ ] `/code-review` を実行し `code-review.md` を作成
