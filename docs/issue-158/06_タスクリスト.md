# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## フェーズ0: 準備（コストなし・低コスト）

- [x] 作業用IAMユーザー（`AdministratorAccess`）を作成し、`~/.aws/config`にプロファイル登録（root運用の廃止）
- [x] IAMロール（ECSタスク実行ロール・ECSコンテナインスタンス用ロール）を作成
- [x] 踏み台SSH用キーペアを作成
- [x] VPC・パブリックサブネット2つ・IGW・ルートテーブルを作成（パブリックIP自動割り当ても有効化済み）
- [x] セキュリティグループ5つ（`sg-bastion`/`sg-alb`/`sg-ecs`/`sg-rds`/`sg-elasticache`）を作成
- [x] ECRリポジトリを作成
- [x] `backend/Dockerfile`でビルドしたイメージをpush
- [x] `frontend-angular`をビルドし、S3バケットへアップロード
- [x] CloudFrontディストリビューション（Price Class 100、OAC設定、S3向けデフォルトビヘイビアのみ）を作成し、デプロイ完了を待つ



## フェーズ1: DB・キャッシュ・踏み台

- [x] RDS用DBサブネットグループ・RDSインスタンス（`db.t4g.micro`、Single-AZ、パブリックアクセス無効）を作成
- [x] ElastiCache用サブネットグループ・Redisクラスタ（`cache.t4g.micro`）を作成
- [x] 踏み台EC2（`t4g.nano`）を起動
- [x] 踏み台経由でRDSへ接続し、Alembicマイグレーション・`backend/seed.sql`投入を実行



## フェーズ2: ECS（backend）

- [x] ECSクラスタ（EC2起動タイプ）・ECSホストEC2（`t4g.nano`）を起動（ASG経由、コンテナインスタンスとして登録済み・利用可能メモリ418MB）
- [x] ECSタスク定義（DATABASE_URL/REDIS_URL/COOKIE_SECURE/COOKIE_SAME_SITE等の環境変数）を登録（リビジョン1）
- [x] ECSサービスを作成し、タスクがRUNNINGになること・CloudWatch Logsにエラーがないことを確認（`t4g.nano`でuvicorn起動成功・DB/Redis接続エラーなし）



## フェーズ3: ALB + CloudFront 連携

- [x] ALB・ターゲットグループ・HTTPリスナーを作成
- [x] ALBのターゲットヘルスが`healthy`になることを確認
- [x] CloudFrontにALBを2つ目のオリジンとして追加し、`/api/*`・`/auth/*`のビヘイビアを設定（オリジンドメインはALBのDNS名、HTTPのみ、キャッシュ無効）
- [x] CloudFront反映後、CloudFront配信URL経由で`/api/...`がALBまで到達することを確認（ログイン成功で実証）
- [x] ECSタスク定義の`COOKIE_SAME_SITE`を`lax`に設定（同一オリジン構成のため）
- [x] **（追加対応）独自ドメイン`secuaigent-ryoyamasuda.tech`をムームードメインで取得、ACM証明書（us-east-1、`*.secuaigent-ryoyamasuda.tech`）を発行**
- [x] CloudFrontに代替ドメイン名`*.secuaigent-ryoyamasuda.tech`を追加し証明書を割り当て
- [x] ムームーDNSにワイルドカードCNAME（`*` → `di4rwgutpepar.cloudfront.net`）を追加



## フェーズ4: 動作確認

- [x] `https://test-tenant.secuaigent-ryoyamasuda.tech/`からログイン成功（Cookie認証含めて機能することを実機ブラウザで確認）
- [ ] `04_テスト設計.md`の残項目（S3直接アクセス遮断・全削除後の残存確認・削除後の課金確認）を実施し`08_動作確認.md`に結果を記録



## フェーズ5: 全リソース削除

- [ ] `03_詳細設計.md`のフェーズ5の順序に従い、ALB→ECS→踏み台→ElastiCache→RDS→CloudFront→S3→ECR→SG→VPC関連の順に削除
- [ ] `aws resourcegroupstaggingapi get-resources --tag-filters Key=Project,Values=issue-158`で0件を確認
- [ ] 翌日以降にAWS Billingで新規課金が発生していないことを確認し、`08_動作確認.md`に記録



## PR・レビュー

- [ ] PR を作成（コード変更がない場合はドキュメントのみのPRとする）
- [ ] `/code-review` を実行し `code-review.md` を作成
