# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `app/models/tenant_resource.py`（`TenantResource`, `TenantResourceType`）を作成
- [x] `alembic/versions/020_add_tenant_resources.py`を作成
- [x] `app/repositories/tenant_resource_repository.py`を作成
- [x] `app/repositories/tenant_repository.py`に`update`を追加
- [x] `app/schemas/tenant.py`（`TenantAdminPatchRequest`, `TenantDetailResponse`他）を作成
- [x] `app/services/tenant_service.py`を作成
- [x] `app/routers/tenants.py`を作成し`app/main.py`に登録

## テスト

- [x] `tests/integration/test_tenants.py`を作成（04・05のケースを実装。テナント不存在の404はFK制約上router経由で再現できないため`tests/unit/test_tenant_service.py`に切り出し）
- [x] `alembic upgrade head`でマイグレーション適用確認（downgrade/upgrade往復も確認）
- [x] `pytest`実行、全件成功を確認（code-review対応後、520 passed）

## 動作確認

- [x] ブラウザ操作は行わず、自動テスト（pytest）の実行結果で代替する旨を`08_動作確認.md`に明記

## PR・レビュー

- [x] PR を作成（`Closes #74`、ベースブランチは`feature/issue-73`である旨を明記。PR #85）
- [x] `/code-review`を実行し`code-review.md`を作成
- [x] 🔴致命的指摘なし（🟡注意・🔵提案のうち5件対応、3件は理由を記載した上で対応しない判断）
