# issue-119 NAW-1172 ユーザー一覧・グループメンバー一覧を利用クレジット順でソート可能にする

## 概要

NAW-1172 の移植。ユーザー管理画面・チーム管理画面のユーザー一覧APIに `includeUsage` パラメータを追加し、有効な請求期間内の `totalCredits`（クレジット利用量）をレスポンスに付与、`sort=totalCredits` でのソートを可能にする。

## 説明

- 対象API: `GET /api/admin/users`、`GET /api/admin/groups/{id}/users`
- 元チケット: NAW-1172
- 移植元コミット: `48307475`（`~/Documents/naw-server`）
- 移植元（バックエンド）:
  - `src/main/java/com/example/naw/api/UserController.java`
  - `src/main/java/com/example/naw/api/GroupController.java`
  - `src/main/java/com/example/naw/service/UserService.java`
  - `src/main/java/com/example/naw/service/GroupService.java`
  - `src/main/java/com/example/naw/service/UserBillingUsageService.java`（新規）
  - `src/main/java/com/example/naw/response/UserResponse.java`
  - `src/main/java/com/example/naw/response/GroupMemberUserResponse.java`
- 前提実装（移植済み）: NAW-1125 クレジット利用状況ダッシュボード
  - `backend/app/routers/credit_usage.py`
  - `backend/app/services/credit_usage_service.py`
  - `backend/app/core/credit_quota.py`（billing period 解決ロジック）

## その他

- スコープはバックエンド（FastAPI）実装のみ。フロントエンド（`frontend/` React移植）は方針上不問。
- Human in the Loop 承認フローは省略し、全自動モードで進める（naw-issue-loop起因タスク）。
