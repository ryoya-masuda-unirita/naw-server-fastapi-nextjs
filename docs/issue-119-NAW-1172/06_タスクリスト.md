# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `app/core/credit_quota.py` に `TOTAL_CREDITS_SORT_PROPERTY`・`is_sort_by_total_credits`・`validate_total_credits_sort`・`total_credits_correlated_subquery` を追加
- [x] `app/repositories/token_usage_repository.py` に `sum_total_credits_by_user_ids` を追加
- [x] `app/schemas/user.py` の `UserResponse` に `totalCredits` を追加
- [x] `app/routers/users.py` に `includeUsage` パラメータを追加し `response_model_exclude_none=True` を付与
- [x] `app/services/user_service.py` の `get_users` に `include_usage` 対応を実装
- [x] `app/repositories/group_user_repository.py` の `find_page_by_group` に `billing_period` 対応を実装
- [x] `app/services/group_service.py` の `list_group_users` に `include_usage` 対応を実装
- [x] `app/routers/groups.py` に `includeUsage` パラメータを追加

## テスト

- [x] `tests/unit/test_credit_quota.py` に `is_sort_by_total_credits`・`validate_total_credits_sort` のテストを追加
- [x] `tests/integration/test_users.py` に `includeUsage`/`sort=totalCredits` のテストを追加
- [x] `tests/integration/test_groups.py` に `includeUsage`/`sort=totalCredits` のテストを追加
- [x] `pytest` 全体実行し、本Issue起因の新規失敗がないことを確認

## 動作確認

- [x] 自動テストの実行結果で代替し `08_動作確認.md` に記録

## PR・レビュー

- [x] PR を作成
- [x] `/code-review` を実行し `code-review.md` を作成
