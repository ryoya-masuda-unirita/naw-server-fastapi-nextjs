# 08_動作確認

## 確認環境

本Issueは全自動モードで対応しており、ブラウザ操作による動作確認は行わない。**自動テスト（pytest）の実行結果のみで動作確認を代替する。**

## 確認手順

```bash
cd backend
pytest tests/integration/test_tenant_endpoints.py -v
```

## 確認結果

（実装後にpytest実行結果を記載する）

## 未確認事項

- ブラウザ（`frontend-angular/`）を用いた実機動作確認は未実施（本Issueの対応方針により自動テストで代替）
