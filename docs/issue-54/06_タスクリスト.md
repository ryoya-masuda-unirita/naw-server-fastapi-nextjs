# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/app/schemas/feedback.py` に feedbackMessage 用スキーマを追加する
- [x] `backend/app/repositories/message_feedback_repository.py` に feedbackMessage 一覧取得用クエリを追加する
- [x] `backend/app/services/feedback_service.py` に `get_feedback_messages` を追加する
- [x] `backend/app/routers/feedback.py` に `GET /api/admin/feedbackMessage` を追加する

## テスト

- [x] `backend/tests/integration/test_feedback.py` に feedbackMessage のテストを追加する
- [x] `pytest` を実行し全テストが通ることを確認する
- [x] `ruff check app tests` を実行し警告がないことを確認する
- [x] `mypy app/` を実行し結果を確認する

## 動作確認

- [x] ローカル環境（`uvicorn` + Docker PostgreSQL）で動作確認する
- [x] `curl` で一覧・絞り込み・権限を確認する
- [x] `08_動作確認.md` に確認結果を記録する

## PR・レビュー

- [x] PR を作成する（`Closes #54` を本文に含める）
- [x] `prepare_code_review.sh` でベース差分を確定し、Codex 自身でレビューする
- [x] `code-review.md` を作成する
- [x] 指摘（🔴 致命的は必須）を修正し、再テスト・再動作確認する
