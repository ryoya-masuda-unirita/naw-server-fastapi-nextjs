# 02_基本設計

## 変更対象

- `backend/app/models/message.py`（変更なし。既にFK制約が正しいことを確認）
- `backend/app/models/room.py`（変更なし。既にFK制約が正しいことを確認）
- `backend/alembic/versions/`（新規マイグレーション追加なし。既存の011・012で必要な制約が満たされている）
- `backend/tests/integration/test_assistants.py`（回帰テストを追加）

## 画面 / API 変更概要

APIの入出力仕様に変更はない。`DELETE /api/admin/assistants/{id}` の挙動（成功時204、使用中ルームがある場合400）は既存のまま。

## 影響範囲

- 実装コードの変更を伴わないため、既存機能への影響はない
- テスト追加のみのため、既存テストへの影響もない

## 移植方針

naw-server コミット `2027e2cbab6283f7fb95e2724645a5f102519f48` が実現しているFK制約（`messages.assistant_id` を `SET NULL`、`rooms.default_assistant_id` を `RESTRICT`）は、FastAPI側で既に実装済みであることを確認した。

そのため本Issueの実装は以下の2点に限定する。

1. 「アシスタント削除時にメッセージが消えずassistant_idがNULLになる」ことを保証する統合テストを追加し、将来のリグレッションを防ぐ
2. 調査結果をドキュメントとして残し、Issue本文の想定スコープ（FK制約変更・マイグレーション追加）が既に満たされていることを明記する

400→409のステータスコード変更は、`01_要件定義.md` に記載の通り別スコープ（未起票のissue-42後続）のため本Issueでは対応しない。
