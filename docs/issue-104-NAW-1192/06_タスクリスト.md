# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成（過去の着手時に作成済み。今回の再開時に現状のbackend実装と矛盾がないことを再確認済み）
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/app/models/message.py`・`backend/app/models/room.py`・Alembicマイグレーションの現状確認（変更不要と結論づけ済み）

## テスト

- [x] `backend/tests/integration/test_assistants.py` に回帰テスト `test_delete_assistant_keeps_messages_and_sets_assistant_id_null` を追加
- [x] 追加テスト・既存の `TestDeleteAssistant` 全体が成功することを確認
- [x] `pytest` 全体（715件）が成功することを確認
- [x] `ruff check` が通過することを確認
- [x] `mypy` の既存エラー（今回の変更ファイル以外）が今回の変更由来でないことを確認

## 動作確認

- [x] `08_動作確認.md` に自動テスト結果を記録（本Issueはroom/messageドメインの内部データ整合性の確認が主目的のため、ブラウザでの実操作確認は行わず、自動テストの実行結果で代替する）

## PR・レビュー

- [x] PR を作成
- [x] `/code-review` を実行し `code-review.md` を作成
