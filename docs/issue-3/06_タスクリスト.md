# 06_タスクリスト

## タスク一覧

- [ ] `backend/pyproject.toml` を作成する
- [ ] `backend/.env.example` を作成する
- [ ] `backend/Dockerfile` を作成する
- [ ] ディレクトリ構造と `__init__.py` を作成する
- [ ] `backend/app/core/config.py` を作成する
- [ ] `backend/app/routers/health.py` を作成する
- [ ] `backend/app/main.py` を作成する
- [ ] `backend/tests/test_health.py` を作成する
- [ ] `pytest` を実行してテストが通ることを確認する
- [ ] `uvicorn` でローカル起動して `/health` が返ることを確認する

## 完了条件

- `pytest` が全テストパスすること
- `uvicorn app.main:app --reload` で起動し `GET /health` が `{"status": "ok"}` を返すこと
