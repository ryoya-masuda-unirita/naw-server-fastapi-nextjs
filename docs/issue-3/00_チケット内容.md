# FastAPI バックエンドの初期セットアップ

## 概要
`backend/` 配下に FastAPI プロジェクトの雛形を作成する。

## やること
- [ ] ディレクトリ構造の作成（routers / services / repositories / models / schemas / core）
- [ ] `pyproject.toml` の作成（依存パッケージ定義）
- [ ] FastAPI アプリのエントリーポイント（`app/main.py`）作成
- [ ] SQLModel + 非同期 SQLAlchemy のセットアップ
- [ ] pydantic-settings による設定管理（`.env` 読み込み）
- [ ] `Dockerfile` の作成
- [ ] ローカル起動確認（`uvicorn app.main:app --reload`）

## 参照
移植元: `~/Documents/naw-server`
