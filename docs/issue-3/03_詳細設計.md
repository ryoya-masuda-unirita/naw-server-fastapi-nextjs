# 03_詳細設計

## 変更ファイル一覧

| ファイル | 種別 |
|---|---|
| `backend/pyproject.toml` | 新規 |
| `backend/.env.example` | 新規 |
| `backend/Dockerfile` | 新規 |
| `backend/app/__init__.py` | 新規 |
| `backend/app/main.py` | 新規 |
| `backend/app/routers/__init__.py` | 新規 |
| `backend/app/routers/health.py` | 新規 |
| `backend/app/services/__init__.py` | 新規 |
| `backend/app/repositories/__init__.py` | 新規 |
| `backend/app/models/__init__.py` | 新規 |
| `backend/app/schemas/__init__.py` | 新規 |
| `backend/app/core/__init__.py` | 新規 |
| `backend/app/core/config.py` | 新規 |

---

## backend/pyproject.toml

```toml
[project]
name = "naw-server-fastapi"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "sqlmodel>=0.0.21",
    "pydantic-settings>=2.6.0",
    "asyncpg>=0.30.0",
    "python-jose[cryptography]>=3.3.0",
    "python-multipart>=0.0.12",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 88
```

---

## backend/.env.example

```env
DATABASE_URL=postgresql+asyncpg://root:root@localhost:7654/postgres
SECRET_KEY=changeme
```

---

## backend/Dockerfile

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv

COPY pyproject.toml .
RUN uv sync --no-dev

COPY app/ ./app/

EXPOSE 8080

CMD ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
```

---

## backend/app/main.py

```python
from fastapi import FastAPI

from app.core.config import settings
from app.routers import health

app = FastAPI(title="naw-server FastAPI", version="0.1.0")

app.include_router(health.router)
```

---

## backend/app/core/config.py

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    database_url: str = "postgresql+asyncpg://root:root@localhost:7654/postgres"
    secret_key: str = "changeme"


settings = Settings()
```

---

## backend/app/routers/health.py

```python
from fastapi import APIRouter

router = APIRouter()


@router.get("/health")
async def health_check() -> dict[str, str]:
    return {"status": "ok"}
```

---

## 空の __init__.py（以下のファイルはすべて空）

- `backend/app/__init__.py`
- `backend/app/routers/__init__.py`
- `backend/app/services/__init__.py`
- `backend/app/repositories/__init__.py`
- `backend/app/models/__init__.py`
- `backend/app/schemas/__init__.py`
- `backend/app/core/__init__.py`
