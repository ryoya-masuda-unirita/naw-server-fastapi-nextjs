# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/app/models/prompt_template.py` に `PromptTemplate`・`GroupPromptTemplate` を作成
- [x] `backend/app/models/group.py` の `GroupUser` docstring を修正（`groups_assistants`・`groups_prompt_templates` の実装状況を最新化）
- [x] `backend/alembic/versions/007_add_prompt_templates.py` を作成（`prompt_templates`・`groups_prompt_templates` テーブル）
- [x] `backend/app/schemas/prompt_template.py` に `PromptTemplateCreateRequest`・`PromptTemplateCreateResponse`・`PromptTemplateResponse`・`PagedPromptTemplateResponse` を作成
- [x] `backend/app/repositories/group_repository.py` に `find_by_tenant_id_and_ids` を追加
- [x] `backend/app/repositories/group_prompt_template_repository.py` を作成
- [x] `backend/app/repositories/prompt_template_repository.py` を作成
- [x] `backend/app/services/prompt_template_service.py` を作成
- [x] `backend/app/routers/prompt_templates.py` を作成（`prompt_template_router`・`admin_prompt_template_router`）
- [x] `backend/app/main.py` にルーターを登録
- [x] `backend/seed.sql` に動作確認用データ（プロンプトテンプレート・グループ紐付け）を追加

## テスト

- [x] `backend/tests/integration/test_prompt_templates.py` を作成（`05_テスト詳細設計.md` の全ケース）
- [x] `backend/tests/unit/test_prompt_template_service.py` を作成
- [x] `pytest` 全体が通ることを確認（186件成功）
- [x] `ruff check app/` が通ることを確認（`mypy`は既存コードも同種のエラーを含み今回のスコープでは未解消。CIでも未使用のため許容）

## 動作確認

- [x] `frontend-angular`（`http://test-tenant.localhost:4201`）経由で一覧・作成・更新・削除の一連の操作を確認
- [x] `08_動作確認.md` に確認内容を記録（チャット画面のテンプレート選択との疎通確認でsize=0の422不具合を発見・修正）

## PR・レビュー

- [ ] PR を作成（`Closes #36`）
- [ ] `/code-review` を実行し `code-review.md` を作成
- [ ] 指摘（🔴）を修正し再テスト・再動作確認
