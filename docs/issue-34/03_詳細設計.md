# 03_詳細設計

## 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `backend/pyproject.toml` | `dev`依存グループに`pre-commit`を追加 |
| `.pre-commit-config.yaml` | 新規作成 |
| `.githooks/pre-push` | 削除 |
| `backend/.claude/CLAUDE.md` | フックの初期設定手順を`pre-commit`向けに書き換え |
| `.github/workflows/backend-tests.yml` | 変更なし |

---

## `backend/pyproject.toml`（差分）

```toml
[dependency-groups]
dev = [
    "aiosqlite>=0.22.1",
    "bcrypt>=4.0.0,<5.0.0",
    "httpx>=0.27.0",
    "mypy>=1.13.0",
    "types-passlib>=1.7.7",
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "pre-commit>=4.0.0",
    "ruff>=0.8.0",
]
```

---

## `.pre-commit-config.yaml`（新規、リポジトリ直下）

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20
    hooks:
      - id: ruff-format
        files: ^backend/
      - id: ruff-check
        args: [--fix]
        files: ^backend/
```

- `files: ^backend/`により、`backend/`配下のファイルのみが対象になる（モノレポで`frontend/`等を巻き込まない）
- `rev`は`backend/pyproject.toml`の`ruff>=0.8.0`と大きく乖離しないバージョンを固定する
- `ruff-check`フックには`args: [--fix]`を指定し、自動修正可能な指摘は修正する。修正できない指摘（構文エラー等）は失敗として扱われる（`id: ruff`は`ruff-check`のlegacy aliasであり、将来的に廃止される可能性があるため、forward-compatibleな`ruff-check`を使用する）

---

## `.githooks/pre-push`（削除）

旧`pre-push`方式の成果物を削除する。`git config core.hooksPath .githooks`を設定していた開発環境では、後述のCLAUDE.mdの案内に従い設定を解除する。

---

## `backend/.claude/CLAUDE.md`（差分）

「開発コマンド」節の「Gitフックの初期設定」を以下に置き換える。

```markdown
### Gitフックの初期設定（初回のみ）

`git commit`時にRuffのフォーマッター・リンターを自動適用するため、`pre-commit`フレームワークを使用する。クローン後に1回だけ実行する。

\`\`\`bash
cd backend && uv sync
uv run pre-commit install
\`\`\`

設定すると、`git commit`のたびに`.pre-commit-config.yaml`で定義したRuffのフォーマット・自動修正（`backend/`配下のみ対象）が実行される。修正が入った場合はそのコミットが失敗するので、変更内容を確認して`git add`し直し、再度`git commit`する（`pre-commit`フレームワークの標準動作）。開発中（保存のたび等）には走らないため、実装途中の未使用importがあっても妨げられない。

なお、以前使用していた`.githooks/pre-push`（`git config core.hooksPath .githooks`）は廃止した。過去にこの設定を行った環境では、以下で設定を解除すること。

\`\`\`bash
git config --unset core.hooksPath
\`\`\`
```

## 実装時の確認事項

- `pre-commit`の`rev`は実装時点でのAstral公式`ruff-pre-commit`リポジトリの最新安定タグを確認して指定する
- `uv run pre-commit install`実行後、`.git/hooks/pre-commit`が生成されることを確認する
- 動作確認は`git log`だけでなく`git ls-remote`でリモートの実態を確認する（旧方式で見落とした反省を踏まえる）
