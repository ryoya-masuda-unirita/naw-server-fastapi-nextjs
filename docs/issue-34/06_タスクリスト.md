# 06_タスクリスト

## クリーンアップ（実施済み・変更なし）

- [x] `backend/pyproject.toml`に`[tool.ruff.lint.per-file-ignores]`を追加する
- [x] `uv run ruff format .`を実行してフォーマットを揃える
- [x] `uv run ruff check --fix .`を実行して自動修正可能な指摘を解消する
- [x] `ruff format --check .`・`ruff check .`が0件になることを確認する
- [x] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## 旧実装の撤去（pre-pushフック方式は技術的に破綻したため）

- [x] `.githooks/pre-push`を削除する
- [x] `backend/.claude/CLAUDE.md`の「Gitフックの初期設定」節を`pre-commit`向けに書き換える

## 実装（pre-commitフレームワーク方式）

- [x] `backend/pyproject.toml`の`dev`依存グループに`pre-commit`を追加する
- [x] `.pre-commit-config.yaml`をリポジトリ直下に新規作成する（`ruff-format`・`ruff --fix`、`backend/`配下限定）
- [x] `uv sync`で`pre-commit`をインストールする

## 動作確認

- [x] `uv run pre-commit install`を実行する
- [x] 意図的にフォーマット崩れ・未使用importを作ってコミットし、1回目は失敗・`git add`し直した2回目で成功することを確認する
- [x] 修正が実際にpushされているか、`git log`だけでなく`git ls-remote`で確認する（前回の見落としの再発防止）
- [x] `feature/issue-34`へのプッシュ後、GitHub Actionsで既存の`lint`ジョブが成功することを確認する
- [x] 検証用コミットは`git reset --soft`＋`git push --force-with-lease`で取り除く

## PR作成・レビュー

- [x] PR作成（`--base develop`、既存PR #35のタイトルを新方式に更新）
- [x] `/code-review` 実行
- [x] `docs/issue-34/code-review.md` 作成
- [x] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- `ruff format --check`・`ruff check`とも指摘0件
- 既存テストに回帰がない
- `pre-commit`フックの自動修正・失敗→再コミットの動作を実機確認済み（`git ls-remote`でリモートの実態まで確認済み）
- GitHub Actionsの`lint`ジョブが成功することを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
