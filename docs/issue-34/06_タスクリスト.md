# 06_タスクリスト

## クリーンアップ

- [x] `backend/pyproject.toml`に`[tool.ruff.lint.per-file-ignores]`を追加する
- [x] `uv run ruff format .`を実行してフォーマットを揃える
- [x] `uv run ruff check --fix .`を実行して自動修正可能な指摘を解消する
- [x] `ruff format --check .`・`ruff check .`が0件になることを確認する
- [x] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## 実装

- [x] `.githooks/pre-push`を新規作成し実行権限を付与する
- [x] `.github/workflows/backend-tests.yml`に`lint`ジョブを追加する
- [x] `backend/.claude/CLAUDE.md`にフックの初期設定手順を追記する

## 動作確認

- [x] ローカルで`git config core.hooksPath .githooks`を設定する
- [x] 意図的にフォーマット崩れを作ってpushし、pre-pushフックが自動修正・amendすることを確認する
- [x] `feature/issue-34`へのプッシュ後、GitHub Actionsで`lint`ジョブが成功することを確認する

## PR作成・レビュー

- [x] PR作成（`--base develop`）
- [x] `/code-review` 実行
- [x] `docs/issue-34/code-review.md` 作成
- [x] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- `ruff format --check`・`ruff check`とも指摘0件
- 既存テストに回帰がない
- pre-pushフックの自動修正・amend動作を実機確認済み
- GitHub Actionsの`lint`ジョブが成功することを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
