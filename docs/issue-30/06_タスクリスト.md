# 06_タスクリスト

## 実装

- [ ] `backend/app/models/assistant.py` に `AssistantEndpoint` モデルを追加する
- [ ] `backend/alembic/versions/006_add_assistants_endpoints.py` を作成し `assistants_endpoints` テーブルを追加する
- [ ] `backend/app/schemas/assistant.py` の `endpoints` 項目を専用スキーマ（`AssistantEndpointItemResponse`）に変更する
- [ ] `backend/app/repositories/assistant_endpoint_repository.py` を新規作成する（バッチ取得でN+1を回避）
- [ ] `backend/app/services/assistant_service.py` を変更し `endpoints` を実データで構築する
- [ ] `backend/seed.sql` に動作確認用テナントエンドポイント・紐付けを追加する

## テスト

- [ ] `tests/integration/conftest.py` のテーブルクリア対象に `assistants_endpoints` を追加する
- [ ] `tests/integration/test_assistants.py` にエンドポイント紐付けのテストを追加する
- [ ] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## マイグレーション適用・動作確認

- [ ] `alembic upgrade head` でマイグレーションを適用する
- [ ] `backend/seed.sql` を再投入する
- [ ] API直叩き（curl等）で `GET /api/assistants` の `endpoints` に実データが返ることを確認する
- [ ] レスポンスに `label`・`apiKey` が含まれないことを確認する

## PR作成・レビュー

- [ ] PR作成（`--base develop`）
- [ ] `/code-review` 実行
- [ ] `docs/issue-30/code-review.md` 作成
- [ ] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- `GET /api/assistants`が実際の接続情報を返すことをAPI直叩きで実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
