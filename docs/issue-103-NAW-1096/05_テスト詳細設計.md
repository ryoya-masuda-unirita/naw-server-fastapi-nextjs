# 05_テスト詳細設計

## テスト対象ファイル

- `backend/tests/integration/test_users.py`

## 追加テストケース

`TestGetUsers`クラスに以下を追加する。

- `test_excludes_system_role_users`
  - USER・ADMIN・SYSTEMロールのユーザーを用意し、`GET /api/admin/users` の結果に
    SYSTEMロールのユーザーが含まれないこと、`totalElements`がSYSTEM除外後の件数と
    一致することを確認する
- `test_role_filter_with_system_returns_empty`
  - `role=SYSTEM`を指定して `GET /api/admin/users` を実行し、`content`が空・
    `totalElements`が0であることを確認する
