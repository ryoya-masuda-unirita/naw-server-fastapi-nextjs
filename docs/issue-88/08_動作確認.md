# 08_動作確認

本Issueは上書きルールにより、ブラウザ操作による動作確認は行わず、自動テスト（pytest）の実行結果のみで代替する。

## 確認環境

- ローカル環境、`pytest`（DB接続不要のunitテストのみ）

## 確認手順

```bash
cd backend
uv run pytest tests/unit/test_credit_quota.py tests/unit/test_llm_client.py tests/unit/test_llm_chat_service.py tests/unit/test_llm_embedding_service.py tests/unit/test_token_usage_credit_calc.py -v
```

## 確認結果

（実装完了後、実際の実行結果をここに追記する）

## 未確認事項

- ブラウザ経由のチャット画面からの実際のストリーミング表示（本Issueでは未確認。frontend-angular側の対応状況にも依存するため、別途確認が必要）
- 実際のAzure OpenAIエンドポイントへの疎通（本Issueではモックのみで検証。実環境の認証情報を要する）
