# 06_タスクリスト

## 実装

- [x] `.github/workflows/backend-tests.yml` を新規作成する

## 動作確認

- [x] ローカルで`.env`退避→`.env.example`コピー→`uv run pytest tests/unit`が成功することを確認する
- [x] ローカルで`.env.example`コピー→`uv run pytest tests/unit tests/integration`が成功することを確認する
- [x] `feature/issue-32`へのプッシュ後、GitHub Actionsで`unit-tests`が成功し`integration-tests`がスキップされることを確認する

## PR作成・レビュー

- [ ] PR作成（`--base develop`）
- [ ] `/code-review` 実行
- [ ] `docs/issue-32/code-review.md` 作成
- [ ] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再動作確認 → PR修正
- [ ] PRマージ後、`develop`へのプッシュで`unit-tests`・`integration-tests`両方が成功することをGitHub Actions上で確認する

## 完了条件

- feature branchへのプッシュで単体テストのみ実行されることを実機確認済み
- `develop`へのマージ後、単体・結合テスト両方が実行され成功することを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
