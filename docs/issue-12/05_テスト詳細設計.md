# 05_テスト詳細設計

## 対象ファイル

- `backend/tests/integration/test_auth.py`

## 実施コマンド

```bash
cd backend
uv run pytest --collect-only tests/integration/test_auth.py
uv run pytest tests/integration/test_auth.py
uv run pytest
```

## 確認内容

- `test_auth.py` の import 解決が正常化し、収集が通ること
- `override_get_session` による依存性差し替えが従来どおり機能すること
- 現行 `httpx` で認証 API integration テスト 8 件が完走すること
- バックエンド全体の `pytest` で、少なくとも本 issue の原因だった収集エラーが解消していること
