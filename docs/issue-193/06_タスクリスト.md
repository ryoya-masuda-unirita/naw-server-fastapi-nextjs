# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `tenant.helpers.ts`: `resolveTenantId()` からサブドメイン抽出を削除し、`persistTenantId()` を追加する
- [x] `user-list-tenant.helpers.ts` を削除する
- [x] `user-list-auth.service.ts`: 共通の `resolveTenantId` を使うよう変更し、repair関連の処理を削除する
- [x] `auth.types.ts`: `LoginRequest` に `tenantId` を追加する
- [x] `auth.store.ts`: `login()` でAPI呼び出し前に `persistTenantId()` を呼ぶ
- [x] `login.component.ts`: テナントIDのフォームコントロール・バリデーション・エラーcomputed・送信処理を追加する
- [x] `login.component.html`: テナントID用の `app-form-input` を追加する
- [x] `ja.json` / `en.json`: `AUTH.LOGIN.TENANT_ID_LABEL` / `TENANT_ID_PLACEHOLDER` を追加する
- [x] `auth.mock.ts`: `sessionStorage` キーの文字列リテラルを `STORAGE_KEYS` 定数経由に修正する
- [x] （追加対応）E2E（Playwright）の全ログインフロー（`e2e/fixtures/auth.fixture.ts` および `e2e/*.spec.ts`）にテナントID入力を追加。テナントID必須化により既存のE2Eログインが軒並み失敗する状態だったため、実装時に判明し追加対応した
- [x] （追加対応）`playwright.config.ts` の `baseURL` からサブドメイン（`test-tenant.localhost`）を削除し `localhost` に変更。`README.md` のサブドメインURL案内も削除しテナントIDはログイン画面で入力する旨に更新

## テスト

- [x] `tenant.helpers.spec.ts` を書き換える（サブドメインスタブ削除、`persistTenantId` のテスト追加）
- [x] `user-list-tenant.helpers.spec.ts` を削除する
- [x] `auth.interceptor.spec.ts` からサブドメインスタブを削除する
- [x] `login.component.spec.ts` にテナントID関連のテストを追加・既存テストを更新する
- [x] `auth.store.spec.ts` の `login` 関連テストを更新する
- [x] `npm test` を実行し、全テストが成功することを確認する（1857件成功）
- [x] `npm run lint` を実行し、エラーがないことを確認する（変更ファイルにエラーなし。既存の77件は本Issueと無関係のファイルの既存エラーで対象外）
- [x] `npx tsc --noEmit` を実行し、型エラーがないことを確認する

## 動作確認

- [ ] `npm run start:mock` または `npm run start:local`（`http://localhost:4201`）でログイン画面にテナントID欄が表示されることを確認する
- [ ] テナントID・ユーザーID・パスワードを入力してログインでき、ダッシュボードへ遷移することを確認する
- [ ] テナントID未入力では送信できず、必須エラーが表示されることを確認する
- [ ] ユーザー管理一覧画面（サブドメイン非依存のテナント解決に変更した箇所）が引き続き正しく表示されることを確認する
- [ ] `loginkey`/`tenantId` クエリ経由のログイン（サブドメイン非依存の既存代替経路）が引き続き動作することを確認する

## PR・レビュー

- [ ] PR を作成する
- [ ] `/code-review` を実行する
- [ ] `code-review.md` を作成する
