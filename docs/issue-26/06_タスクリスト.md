# 06_タスクリスト

## 実装

- [x] `backend/app/models/assistant.py` に `AssistantType`・`Assistant`・`GroupAssistant` モデルを追加する
- [x] `backend/alembic/versions/004_add_assistants.py` を作成し `assistants`・`groups_assistants` テーブルを追加する
- [x] `backend/app/repositories/assistant_repository.py` を新規作成する
- [x] `backend/app/repositories/group_assistant_repository.py` を新規作成する（アシスタントごとのグループID取得はN+1を避けるバッチ取得方式にする）
- [x] `backend/app/schemas/assistant.py` を新規作成する
- [x] `backend/app/services/assistant_service.py` を新規作成する
- [x] `backend/app/routers/assistants.py` を新規作成する
- [x] `backend/app/main.py` に新規ルーターを登録する
- [x] `backend/seed.sql` にアシスタント・グループ紐付けのシードデータを追加する

## テスト

- [x] `tests/integration/conftest.py` のテーブルクリア対象に `groups_assistants`・`assistants` を追加する
- [x] `tests/integration/test_assistants.py` を新規作成する
- [x] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## マイグレーション適用・動作確認

- [x] `alembic upgrade head` でマイグレーションを適用する
- [x] `backend/seed.sql` を再投入する
- [x] API直叩き（curl等）で `GET /api/assistants` のレスポンスを確認する
- [x] `frontend-angular`（`http://localhost:4201`）経由でダッシュボードのアシスタント一覧の404が解消されることをPlaywrightで確認する

## PR作成・レビュー

- [ ] PR作成（`--base develop`）
- [ ] `/code-review` 実行
- [ ] `docs/issue-26/code-review.md` 作成
- [ ] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- `frontend-angular` 経由でダッシュボードのアシスタント一覧の404が解消されることを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
