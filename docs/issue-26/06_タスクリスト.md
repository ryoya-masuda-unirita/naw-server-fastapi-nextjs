# 06_タスクリスト

## 実装

- [ ] `backend/app/models/assistant.py` に `AssistantType`・`Assistant`・`GroupAssistant` モデルを追加する
- [ ] `backend/alembic/versions/004_add_assistants.py` を作成し `assistants`・`groups_assistants` テーブルを追加する
- [ ] `backend/app/repositories/assistant_repository.py` を新規作成する
- [ ] `backend/app/repositories/group_assistant_repository.py` を新規作成する（アシスタントごとのグループID取得はN+1を避けるバッチ取得方式にする）
- [ ] `backend/app/schemas/assistant.py` を新規作成する
- [ ] `backend/app/services/assistant_service.py` を新規作成する
- [ ] `backend/app/routers/assistants.py` を新規作成する
- [ ] `backend/app/main.py` に新規ルーターを登録する
- [ ] `backend/seed.sql` にアシスタント・グループ紐付けのシードデータを追加する

## テスト

- [ ] `tests/integration/conftest.py` のテーブルクリア対象に `groups_assistants`・`assistants` を追加する
- [ ] `tests/integration/test_assistants.py` を新規作成する
- [ ] 既存の全テスト（`pytest`）が回帰なく通ることを確認する

## マイグレーション適用・動作確認

- [ ] `alembic upgrade head` でマイグレーションを適用する
- [ ] `backend/seed.sql` を再投入する
- [ ] API直叩き（curl等）で `GET /api/assistants` のレスポンスを確認する
- [ ] `frontend-angular`（`http://localhost:4201`）経由でダッシュボードのアシスタント一覧の404が解消されることをPlaywrightで確認する

## PR作成・レビュー

- [ ] PR作成（`--base develop`）
- [ ] `/code-review` 実行
- [ ] `docs/issue-26/code-review.md` 作成
- [ ] 指摘修正（🔴必須、🟡🔵は判断のうえ対応）→ 再テスト → 再動作確認 → PR修正

## 完了条件

- 新規追加テストがすべて成功する
- 既存テストに回帰がない
- `frontend-angular` 経由でダッシュボードのアシスタント一覧の404が解消されることを実機確認済み
- `/code-review` の🔴致命的指摘がすべて解消されている
