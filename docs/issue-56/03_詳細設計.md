# 03_詳細設計

## 実装対象ファイル一覧

| 種別 | ファイル | 内容 |
|---|---|---|
| model | `backend/app/models/room.py` | 変更なし（既存の `RoomRating` / `Room.rating` を利用） |
| schema | `backend/app/schemas/room.py` | `RoomFeedbackCreateRequest` を追加 |
| schema | `backend/app/schemas/feedback.py` | `FeedbackRoomListRequest` / `RoomInfoResponse` / `FeedbackRoomItemResponse` / `PagedFeedbackRoomResponse` / `FeedbackRoomListResponse` を追加 |
| repository | `backend/app/repositories/room_repository.py` | `find_feedback_rooms` を追加 |
| service | `backend/app/services/room_service.py` | `feedback_room` を追加 |
| service | `backend/app/services/feedback_service.py` | `get_feedback_rooms` を追加 |
| router | `backend/app/routers/rooms.py` | `POST /{room_id}/feedback` を追加 |
| router | `backend/app/routers/feedback.py` | `GET /feedbackRoom` を追加 |

## schema

### `backend/app/schemas/room.py`

```python
from app.models.room import RoomRating


class RoomFeedbackCreateRequest(BaseModel):
    rating: RoomRating
```

### `backend/app/schemas/feedback.py`

`_FEEDBACK_MESSAGE_SORT_FIELD_VALUES` と同様の形で、feedbackRoom 用の許容値集合を追加する。

```python
_FEEDBACK_ROOM_SORT_FIELD_VALUES = {"updatedAt", "chat", "name", "level"}


class FeedbackRoomListRequest(BaseModel):
    page: int = Field(default=0, ge=0)
    size: int = Field(default=20, ge=1, le=100)
    assistantId: str | None = None
    rating: RoomRating | None = None
    sortField: str = "updatedAt"
    sortOrder: str = "desc"

    @field_validator("sortField")
    @classmethod
    def _validate_feedback_room_sort_field(cls, value: str) -> str:
        """feedbackRoom用のsortFieldが許容値のいずれかであることを検証する。"""
        if value not in _FEEDBACK_ROOM_SORT_FIELD_VALUES:
            raise ValueError("sortField は updatedAt, chat, name, level のいずれかです")
        return value

    @field_validator("sortOrder")
    @classmethod
    def _validate_feedback_room_sort_order(cls, value: str) -> str:
        """feedbackRoom用のsortOrderがasc/descのいずれかであることを検証する。"""
        if value not in _SORT_ORDER_VALUES:
            raise ValueError("sortOrder は asc または desc です")
        return value


class RoomInfoResponse(BaseModel):
    id: str
    name: str | None
    defaultAssistantId: str | None
    defaultAssistantName: str | None
    rating: RoomRating


class FeedbackRoomItemResponse(BaseModel):
    id: str
    tenantId: str
    userId: str
    userName: str | None
    roomId: str
    room: RoomInfoResponse
    rating: RoomRating
    createdAt: datetime
    updatedAt: datetime


class PagedFeedbackRoomResponse(BaseModel):
    content: list[FeedbackRoomItemResponse]
    totalElements: int
    number: int
    size: int


class FeedbackRoomListResponse(BaseModel):
    feedbacks: PagedFeedbackRoomResponse
```

`RoomRating` は `app/models/room.py` からインポートする（`MessageRating` と同じ扱い）。

移植元 `FeedbackRoomResponse` にある `indexId` はレスポンスに含めない（`01_要件定義.md` のスコープ外方針どおり、files ドメイン未移植のため）。フロントエンド型 (`RoomFeedback.indexId?: string`) も optional のため影響なし。

## repository

### `backend/app/repositories/room_repository.py` に追加

`MessageFeedbackRepository.find_feedback_messages` と同じ形（select + outerjoin + where + count + order_by + offset/limit）。`Room` は 1 テーブルで `User`（所有者名）・`Assistant`（デフォルトアシスタント名）と outerjoin する。

```python
from app.models.assistant import Assistant


@staticmethod
async def find_feedback_rooms(
    tenant_id: str,
    assistant_id: str | None,
    rating: RoomRating | None,
    sort_column: str,
    sort_desc: bool,
    page: int,
    size: int,
    session: AsyncSession,
) -> tuple[list[Row], int]:
    """評価済みルームのフィードバック一覧を取得する。"""
    stmt = (
        select(
            Room.id.label("room_id"),
            Room.tenant_id.label("tenant_id"),
            Room.user_id.label("user_id"),
            Room.name.label("room_name"),
            Room.default_assistant_id.label("assistant_id"),
            Room.rating.label("rating"),
            Room.created_at.label("created_at"),
            Room.updated_at.label("updated_at"),
            User.name.label("user_name"),
            Assistant.name.label("assistant_name"),
        )
        .select_from(Room)
        .outerjoin(
            User, and_(User.id == Room.user_id, User.tenant_id == tenant_id)
        )
        .outerjoin(
            Assistant,
            and_(
                Assistant.id == Room.default_assistant_id,
                Assistant.tenant_id == tenant_id,
            ),
        )
        .where(Room.tenant_id == tenant_id, Room.rating.is_not(None))
    )

    if assistant_id is not None:
        stmt = stmt.where(Room.default_assistant_id == assistant_id)
    if rating is not None:
        stmt = stmt.where(Room.rating == rating)

    total = (
        await session.execute(select(func.count()).select_from(stmt.subquery()))
    ).scalar() or 0

    if sort_column == "room_name":
        order_col = Room.name
    elif sort_column == "assistant_id":
        order_col = Room.default_assistant_id
    elif sort_column == "rating":
        order_col = Room.rating
    else:
        order_col = Room.updated_at

    stmt = stmt.order_by(order_col.desc() if sort_desc else order_col.asc())
    stmt = stmt.offset(page * size).limit(size)
    result = await session.execute(stmt)
    return list(result.all()), total
```

`sortField` → 実カラムのマッピングは `02_基本設計.md` のとおり `FeedbackService` 側で解決し、`sort_column` にはリポジトリ内部名（`room_name` / `assistant_id` / `rating` / `updated_at`）を渡す。

## service

### `backend/app/services/room_service.py` に追加

既存の `_get_owned_room_or_404`（`pin_room` 等と同じ所有者チェック）を再利用する。

```python
@staticmethod
async def feedback_room(
    room_id: str,
    tenant_id: str,
    current_user: User,
    rating: RoomRating,
    session: AsyncSession,
) -> None:
    """ルーム所有者が満足度評価を登録する。"""
    room = await RoomService._get_owned_room_or_404(
        room_id, tenant_id, current_user, session
    )
    room.rating = rating
    session.add(room)
    await session.commit()
```

### `backend/app/services/feedback_service.py` に追加

`get_feedback_messages` と同じ形。

```python
_FEEDBACK_ROOM_SORT_FIELD_TO_COLUMN = {
    "updatedAt": "updated_at",
    "chat": "room_name",
    "name": "assistant_id",
    "level": "rating",
}


@staticmethod
async def get_feedback_rooms(
    tenant_id: str, query: FeedbackRoomListRequest, session: AsyncSession
) -> FeedbackRoomListResponse:
    """テナント内の評価済みルーム一覧を取得する。"""
    sort_column = _FEEDBACK_ROOM_SORT_FIELD_TO_COLUMN[query.sortField]
    rows, total = await RoomRepository.find_feedback_rooms(
        tenant_id,
        query.assistantId,
        query.rating,
        sort_column,
        query.sortOrder == "desc",
        query.page,
        query.size,
        session,
    )

    content = [
        FeedbackRoomItemResponse(
            id=row.room_id,
            tenantId=row.tenant_id,
            userId=str(row.user_id),
            userName=row.user_name,
            roomId=row.room_id,
            room=RoomInfoResponse(
                id=row.room_id,
                name=row.room_name,
                defaultAssistantId=row.assistant_id,
                defaultAssistantName=row.assistant_name,
                rating=row.rating,
            ),
            rating=row.rating,
            createdAt=row.created_at,
            updatedAt=row.updated_at,
        )
        for row in rows
    ]

    return FeedbackRoomListResponse(
        feedbacks=PagedFeedbackRoomResponse(
            content=content,
            totalElements=total,
            number=query.page,
            size=query.size,
        )
    )
```

移植元は `id`（フィードバックID）にルームIDをそのまま使っている（`feedbackRoom` はルーム自体を評価対象とし、専用フィードバックテーブルを持たないため）。この移植でも `id = roomId` とする。

## router

### `backend/app/routers/rooms.py` に追加

既存 `pin_room` と同じ形。

```python
@router.post("/{room_id}/feedback", status_code=status.HTTP_204_NO_CONTENT)
async def feedback_room(
    room_id: str,
    req: RoomFeedbackCreateRequest,
    x_tenant_id: str = Depends(get_verified_tenant_id),
    current_user: User = Depends(get_current_user),
    session: AsyncSession = Depends(get_session),
) -> None:
    await RoomService.feedback_room(
        room_id, x_tenant_id, current_user, req.rating, session
    )
```

### `backend/app/routers/feedback.py` に追加

既存 `get_feedback_messages` と同じ形。

```python
@router.get("/feedbackRoom", response_model=FeedbackRoomListResponse)
async def get_feedback_rooms(
    query: Annotated[FeedbackRoomListRequest, Query()],
    x_tenant_id: str = Depends(get_verified_tenant_id),
    current_user: User = Depends(require_admin_or_group_admin),
    session: AsyncSession = Depends(get_session),
) -> FeedbackRoomListResponse:
    return await FeedbackService.get_feedback_rooms(x_tenant_id, query, session)
```

## 権限判定

- `POST /{room_id}/feedback`: `get_current_user` + `RoomService._get_owned_room_or_404`（既存、所有者以外は404）
- `GET /feedbackRoom`: `require_admin_or_group_admin`（既存、テナント管理者・グループ管理者以外は403）

## 不明点・仕様差分（参照実装を読んだ結果）

- 移植元 `FeedbackRoomResponse.room.rating` と `FeedbackRoomResponse.rating` は同じ値が重複して入る（Java実装をそのまま踏襲し、この移植でも重複させる）
- `defaultAssistantName` はデフォルトアシスタントが削除済み等で解決できない場合 `null` になる（`outerjoin` のため）
