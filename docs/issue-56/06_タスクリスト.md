# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `backend/app/schemas/room.py` に `RoomFeedbackCreateRequest` を追加
- [x] `backend/app/schemas/feedback.py` に `FeedbackRoomListRequest` / `RoomInfoResponse` / `FeedbackRoomItemResponse` / `PagedFeedbackRoomResponse` / `FeedbackRoomListResponse` を追加
- [x] `backend/app/repositories/room_repository.py` に `find_feedback_rooms` を追加
- [x] `backend/app/services/room_service.py` に `feedback_room` を追加
- [x] `backend/app/services/feedback_service.py` に `get_feedback_rooms` を追加
- [x] `backend/app/routers/rooms.py` に `POST /{room_id}/feedback` を追加
- [x] `backend/app/routers/feedback.py` に `GET /feedbackRoom` を追加

## テスト

- [x] `backend/tests/integration/test_rooms.py` に `TestFeedbackRoom` を追加
- [x] `backend/tests/integration/test_feedback.py` に `TestGetFeedbackRooms` を追加
- [x] `uv run pytest tests/integration/test_rooms.py tests/integration/test_feedback.py -v` が全件成功することを確認（62件成功、全体342件成功）
- [x] `uv run ruff check` / `uv run mypy` 等、既存の lint・型チェックが通ることを確認（mypyの既存エラーは本Issue対象コード以外の既存差分と同型のもので、ベースライン216件→242件と同パターンの増加のみ）

## 動作確認

- [ ] `frontend-angular`（`http://localhost:4201`）でルーム満足度評価の登録と、管理画面のフィードバック一覧（ルーム）表示を確認

## PR・レビュー

- [ ] PR を作成（`Closes #56`）
- [ ] `/code-review` を実行
- [ ] `code-review.md` を作成
- [ ] 指摘（🔴 は必須、🟡🔵 は判断）に対応
