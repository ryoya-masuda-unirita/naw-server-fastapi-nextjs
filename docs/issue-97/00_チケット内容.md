# #97 issue-97 メッセージ添付ファイル(attachmentFiles/historyAttachmentFiles)対応

## 概要

Chat/Messageエンドポイント共通で、添付ファイル（`attachmentFiles`/`historyAttachmentFiles`）の送信・LLMへの引き渡しに対応する。

## 説明

issue-88（`POST /api/llm/chat`, `POST /api/llm/embedding`）・issue-93（`POST /api/messages/content`）の両方で、添付ファイル対応は意図的にスコープ外とされていた。本Issueでその添付ファイル対応を行う。

- リクエストへの`attachmentFiles`/`historyAttachmentFiles`受け取り
- LLM呼び出し時のファイル内容の引き渡し

## その他

- 参照調査: `docs/調査結果/20260711.md`（4章 項目2）
- 移植元（バックエンド）: `~/Documents/naw-server/src/main/java/com/example/naw/service` 配下の添付ファイル関連実装
- 移植先: `backend/app/services/message_service.py`, `backend/app/services/llm_chat_service.py`
- 関連Issue: #88, #93（スコープ外節を参照）
