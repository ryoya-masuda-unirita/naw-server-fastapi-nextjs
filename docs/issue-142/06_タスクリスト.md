# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 実装

- [x] `develop` 未マージの Issue #140/#141 相当の変更（`backend/pyproject.toml` の mypy 設定、`index_service.py`・`message_service.py` の型エラー修正）を `feature/issue-141` から cherry-pick で先行反映
- [x] `.github/workflows/backend-tests.yml` に `mypy` ジョブを追加（既存の `lint` ジョブと同構成、実行コマンドは `uv run mypy app tests`）

## テスト

- [x] `uv run mypy app tests` を実行し、対象範囲でエラー0件であることを確認
- [x] `uv run pytest tests/unit -q` を実行し、既存テストが全件成功することを確認
- [x] `uv run ruff format --check .`・`uv run ruff check .` を実行し、既存のフォーマット・リンターチェックに影響がないことを確認
- [ ] PR作成後、GitHub Actions上で `mypy` ジョブを含む全ジョブが成功することを確認

## 動作確認

- [ ] `08_動作確認.md` に、自動テスト・mypy実行結果・CI実行結果による確認内容を記録（本Issueはブラウザ操作を伴わないCI設定変更のため、ブラウザでの動作確認は対象外）

## PR・レビュー

- [ ] PR を作成（`Closes #142`）
- [ ] `/code-review` を実行し `code-review.md` を作成
