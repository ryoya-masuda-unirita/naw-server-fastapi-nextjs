# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`・`04_テスト設計.md` を作成

## カテゴリ別実装

進行中のカテゴリごとに、仕分け結果（自動化対象/対象外）・実装ファイル・テスト結果を記載する。

### 04_ログアウト（10項目）→ 完了

- [x] テスト項目の仕分け（項番8/9はChrome実行1本＋Edge検証観点なしのため個別テスト化せず。他8項目は自動化）
- [x] `frontend-angular/e2e/logout.spec.ts` 実装（7テストケース）
- [x] `npx playwright test`で全件PASS確認（7 passed）

### 05_パスワード再設定（38項目）→ 完了（アプリ側バグにより一部skip）

- [x] テスト項目の仕分け
  - 項番4（Chrome動作確認）: 項番3と同一手順のためplaywright.config.tsのchromiumプロジェクト実行で代表、個別テスト化せず
  - 項番22（試行回数上限）: 連続失敗による状態変化が他テストに影響しうるため対象外
  - 項番23〜38（パスワードポリシー組み合わせ16パターン）: backend側の`verify_password_strength()`が`pw_policy_min_length`のみ検証しており、大文字/小文字/数字/記号の複雑性ポリシーを一切検証していないため対象外。移植漏れの可能性が高く **Issue #168** として別途起票
  - 残り20項目を自動化対象とし`password-reset.spec.ts`に実装（画面表示3・バリデーション10・正常系異常系5、計18テストケース）
- [x] `frontend-angular/e2e/password-reset.spec.ts` 実装
  - パスワード欄は表示切替トグルで`type`属性自体が変化するため、`input[type="password"]`ではなく`input`全体を固定順の絶対インデックスで参照する方式を採用
- [x] `npx playwright test`で結果確認（13 passed, 5 skipped）
  - **【致命的バグ発見】** パスワード再設定APIが常時422エラーで機能していない。frontendの送信フィールド名(`username`)とbackendの期待フィールド名(`loginId`)が不一致（`frontend-angular/src/app/features/auth/types/auth.types.ts`の`ResetPasswordRequest.username` / `backend/app/schemas/auth.py`の`PasswordResetRequest.loginId`）。これにより「正常更新」「更新後の再ログイン」「旧パスワード誤り」「存在しないログインID」「新旧パスワード同一」の5テストが本来検証したい分岐に到達できず、**Issue #167** として別途起票し、該当5テストは`test.skip`＋コメントで記録
  - 本Issue(#166)のスコープはテスト自動化基盤であり、アプリ本体（#167, #168）の修正は行わない
  - `backend/seed.sql`に「新旧パスワード同一」確認用の`password-reuse-user`（12文字以上のパスワードを持つ専用ユーザー）を追加

### 08_チャット検索（15項目）→ 完了

- [x] テスト項目の仕分け
  - 項番15（Chrome動作確認）: 項番3と同一手順のためplaywright.config.tsのchromiumプロジェクト実行で代表、個別テスト化せず
  - 残り14項目を自動化対象とし`chat-search.spec.ts`に実装（14テストケース）
- [x] `frontend-angular/e2e/chat-search.spec.ts` 実装
  - サイドバーの最近のチャット一覧にも同名ルームが表示されるため、チャット名でのアサーションは`role="dialog"`内に必ずスコープする方式を採用
  - 項番8（ページ送り）用に`backend/seed.sql`へ「検索確認用ルーム01」〜「25」（25件、user01所有）を追加
  - 【i18nバグ発見】共通クリアボタン(`app-search-input`)のaria-labelが未定義キー`COMMON.CLEAR`を参照しており翻訳されず、生のキー文字列がそのまま表示される。Issue #169として別途起票し、本Issueのスコープ外のためテストは実際の挙動（キー文字列）に合わせて記録
- [x] `npx playwright test`で全件PASS確認（14 passed）

### 12_ワークスペース設定（16項目）→ 完了

- [x] テスト項目の仕分け
  - 項番16（Chrome動作確認）: 項番1と同一手順のためchromiumプロジェクト実行で代表、個別テスト化せず
  - 項番15（API追加→アシスタント→学習データ）: アシスタント作成ダイアログでの接続先反映確認（項番10と同一のエンドポイント一覧取得APIを参照）をもって代表させ、学習データ画面まで含めたフルチェーンの検証はスコープ外（学習データ画面固有のE2Eは別カテゴリで対応）
  - 残り14項目を自動化対象とし`workspace-settings.spec.ts`に実装（14テストケース）
- [x] `frontend-angular/e2e/workspace-settings.spec.ts` 実装
  - このカテゴリは管理者権限が必要なため、user01ではなくadminでログインするヘルパーを実装
  - テナント名・API一覧という共有状態を変更するテストのため、`test.describe.configure({ mode: 'serial' })`で直列実行に設定
  - `app-form-input`のラベルは`for`属性が実inputの`id`と紐付いておらずgetByLabelが使えないため、`getByRole('textbox', {name: placeholder})`で取得
  - ワークスペース名変更ボタン(鉛筆アイコン)はアクセシブルネームがボタン自体に伝播しないため、見出し要素の直後の兄弟要素として取得
  - 不正URL(項番13)は`backend/app/services/tenant_endpoint_service.py`の`_assert_valid_url`（http(s)://で始まらないと400）を利用して検証
- [x] `npx playwright test`で全件PASS確認（14 passed）

### 10_チャットルーム（16項目）→ 完了

- [x] テスト項目の仕分け
  - 項番5/6（ピン留め/解除 Chrome）: 項番3/4と同一手順のためchromiumプロジェクト実行で代表、個別テスト化せず
  - 項番8（個別削除 Chrome）: 項番7と同一手順のため同上の理由で個別テスト化せず
  - 残り13項目を自動化対象とし`chat-room.spec.ts`に実装（11テストケース。項番11/12は項番3/4で、項番15/16は専用ケースで実質カバー）
- [x] `frontend-angular/e2e/chat-room.spec.ts` 実装
  - 名前変更・ピン留め・削除でサイドバーの共有状態を破壊的に変更するため`test.describe.configure({ mode: 'serial' })`で直列実行
  - サイドバーのチャット一覧は取得件数に上限があり、`chat-search.spec.ts`用に追加した「検索確認用ルーム」25件で古いルーム（フィードバック確認用ルーム等）が一覧から押し出され表示されないことが判明。本カテゴリ専用に`backend/seed.sql`へ「チャットルーム操作確認用ルーム1/2/3」を追加（ルーム3は「表示中チャット削除」テスト専用、削除で恒久的に消えるルーム1とは分離）
  - ルームメニュー起動ボタンは`group-hover`でホバー時のみ表示されるため`hover()`＋`scrollIntoViewIfNeeded()`を使用
  - サイドバーの「チャットの名前を変更」メニュー項目はチャットヘッダー側にも同名の項目があり重複するため、`getByRole('complementary')`（サイドバー領域）にスコープして解決
- [x] `npx playwright test`で全件PASS確認（11 passed）

### 20〜22_Waha!生成AI（11項目）→ 対象外（Playwright自動化不可）

- [x] テスト項目の仕分け（全11項目を対象外と判定、実装なし）
  - このカテゴリはSecuAiGent本体（Webアプリ）ではなく、外部ツール「Waha!」（Windows上で動くジョブランナー）がSecuAiGentのAPIをバッチ的に呼び出す連携テストであることを確認した
  - テスト方法は「Waha!側の各ジョブ（Chat/RAG/MCP/学習/削除/検索）を実行する」、確認内容は「Waha!側の出力ファイル(`c:\temp\*.txt`, `*.json`, `*.tsv`)の中身」「PostgreSQLの`embeddinggraph_vectors4`等のテーブルの中身」であり、いずれもブラウザ操作・Web UI表示を一切経由しない
  - Playwrightはブラウザ自動操作ツールであり、Windows上の別プロセス（Waha!）の実行やローカルファイルシステム・DB内容の直接検証はスコープ外のため、E2E自動化の対象外とする
  - 本Issue(#166)のスコープはブラウザE2Eテスト自動化基盤であり、Waha!連携の自動テスト化は別途の技術検討（APIレベルの結合テスト等）が必要と判断し、実装は行わない

### 06_レイアウト（29項目）→ 完了

- [x] テスト項目の仕分け
  - 項番19（Chrome動作確認）: 項番1と同一手順のためchromiumプロジェクト実行で代表、個別テスト化せず
  - 項番15（既存チャット一覧表示）: `chat-room.spec.ts`のルーム遷移系テストで実質カバー済みのため重複させず
  - 項番20/21（一般/管理者ユーザーメニュー構成）: 項番2/3/11/12の個別確認で実質カバー済みのため重複させず
  - 項番23（チャット検索ボタン表示）: `chat-search.spec.ts`の「検索モーダル表示」テストで実質カバー済みのため重複させず
  - 項番26（データ活用折りたたみ）: 項番5と操作対象・確認内容が同一のトグル動作のため、項番5のテストで開閉両方向をまとめて検証
  - 残り21項目を自動化対象とし`layout.spec.ts`に実装（21テストケース）
- [x] `frontend-angular/e2e/layout.spec.ts` 実装
  - 項番4（新規チャット遷移）: 移植元の備考で当時の担当者が許容済みの既知の挙動として記録されている通り、現在の実装（`sidebar.component.ts` `openNewChatDialog()`）も直接URL遷移ではなくアシスタント選択ダイアログを開く仕様のため、その実際の挙動を検証する形にした
  - 項番28（一般ユーザー管理画面直アクセス）: `admin.guard.ts`はUrlTreeなしのfalseを返す実装のため、Angular Routerによりナビゲーションがキャンセルされ`/admin/users`以外（現状`/chat/new`）にフォールバックする。この実際の挙動に合わせて検証する
  - サブメニューの開閉状態はテキストの表示/非表示ではなく`overflow-hidden`コンテナの`style="height: ...px"`で管理されているため、その値で開閉判定する
  - `chat-room.spec.ts`のサイドバーメニュー起動処理（`group-hover`表示ボタン）が全体テスト並列実行時にCPU負荷でホバー反映が遅れ稀に失敗することが判明したため、hoverをリトライする形に補強した
- [x] `npx playwright test`で全件PASS確認（21 passed）、全体テスト実行でも92 passed / 6 skipped で回帰なしを確認

### 19_テンプレート（29項目）→ 完了

- [x] テスト項目の仕分け
  - 項番29（Chrome動作確認）: 項番1と同一手順のためchromiumプロジェクト実行で代表、個別テスト化せず
  - 項番15（ページ送り・下部）: 項番14と同一のページング機構のため上部/下部両方を1テストで検証
  - 項番23（テンプレート0件）: 項番7（検索0件）と同一の空状態表示コンポーネントのため統合
  - 項番25（作成→チャット利用）: 項番22（チャット連携）と統合。実際のメッセージ送信・AI応答の検証は07_チャット送信カテゴリのスコープとした
  - 項番10（所属チームフィルター特定チーム）: 移植元の備考にあった過去の課題「特定チームの選択ができない」は実装確認の結果解消されていたため、実際に動作する機能として検証
  - 残り24項目を自動化対象とし`templates.spec.ts`に実装（24テストケース）
- [x] `frontend-angular/e2e/templates.spec.ts` 実装
  - ページ送り確認用に`backend/seed.sql`へ「ページ送り確認用テンプレート01〜09」（9件）を追加
  - 【既知の不具合】並べ替えUI（名前順/更新日時順、昇順/降順）を操作しても表示順が一切変化しない不具合を発見。frontend側`template-list.store.ts`がsortField/sortOrderをAPIに送信しておらず、backend側`find_page_for_admin`もソートパラメータを受け取らずupdated_at降順固定になっているため。Issue #170として別途起票し、テストは実際の挙動（変化しないこと）に合わせて記録
  - 【既知の不具合】テンプレート名のフロントエンドバリデーション上限(100文字)とDBカラム上限(32文字)が不一致で、33〜100文字の名前だと作成に失敗する不具合を発見。Issue #171として別途起票し、テストは実際に保存可能な32文字で検証
  - チャット画面のテンプレート選択API(`GET /api/prompt-templates`)は「所属グループに紐づくテンプレートのみ」返す仕様のため、動作確認用グループ未所属のadminではなくuser01でログインして検証
  - `app-form-sort-input`はトリガーがラベルではなく選択値を表示するdiv、選択肢はrole="option"ではなく素のbuttonという実装のため、それに合わせたセレクタを使用
- [x] `npx playwright test`で全件PASS確認（24 passed）。全体テスト実行でも116 passed / 6 skipped で回帰なしを2回連続確認
  - あわせて`chat-room.spec.ts`のgroup-hoverメニュー操作が並列フル実行時にCPU負荷で稀に失敗する既存の不安定性を発見し、hover+クリックを一括リトライする形に強化して解消（`playwright.config.ts`にexpectタイムアウトを8000msへ延長する変更も実施）

### 13_ユーザー管理（24項目）→ 完了

- [x] テスト項目の仕分け
  - 項番21/22（Chrome/Edge動作確認）: 項番1と同一手順のためchromiumプロジェクト実行で代表、個別テスト化せず
  - 項番19（作成APIエラー）: 項番5（重複ID）と同一のエラーハンドリング経路のため統合
  - 項番20（削除APIエラー）: UI操作だけで削除APIエラーを意図的に発生させる手段がなく対象外
  - 項番23/24（ログインキーでのログイン）: バックエンドに`POST /api/auth/login-key`は実装済みだが、フロントエンドにこれを呼び出すUIが一切存在しない（`AuthApiService.loginWithKey()`がどこからも呼ばれていない）ため、ログインキーが発行されユーザー編集画面で保持されることまでを検証し、実際のログイン可否検証は対象外
  - 残り19項目を自動化対象とし`user-management.spec.ts`に実装（19テストケース）
- [x] `frontend-angular/e2e/user-management.spec.ts` 実装
  - ページ送り確認用に`backend/seed.sql`へ「ページ送り確認用ユーザー01〜05」を追加
  - 【既知の不具合】並べ替えUIを操作しても表示順が変化しない不具合を発見。`backend/app/repositories/user_repository.py`の一覧取得クエリにorder_byが一切実装されていないため。Issue #172として別途起票（Issue #170と同一の根本原因パターン）
  - 追加ダイアログは作成ボタンの事前無効化ではなくクリック時のインラインエラー表示方式のため、それに合わせた検証方式を採用
  - ログインキー欄はラベル・placeholderともになくアクセシブルネームを持たない実装のため、フォーム内の位置（3番目のテキスト入力）で特定
- [x] `npx playwright test`で全件PASS確認（19 passed）。全体テスト実行では実行のたびに無関係な別テストで一過性の失敗が発生することを確認したが、いずれも既知の環境要因（フル並列実行時のCPU負荷、`chat-room.spec.ts`と同種の問題）によるものであり、単独実行では安定してPASSすることを確認した

### 16_チャット履歴（25項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/chat-history.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 09_ライブラリ（41項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/library.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 17_学習データ（47項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/learning-data.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 15_アシスタント（48項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/assistants.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 18_フィードバック管理（52項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/feedback.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 07_チャット送信（55項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/chat-send.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 14_チーム管理（71項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/team-management.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

### 11_チャット詳細（77項目）

- [ ] テスト項目の仕分け
- [ ] `frontend-angular/e2e/chat-detail.spec.ts` 実装
- [ ] `npx playwright test`で全件PASS確認

## テスト（全体）

- [ ] backend: `pytest tests/unit` 全件成功を確認
- [ ] frontend-angular: `npm test`（vitest）全件成功を確認
- [ ] frontend-angular: `npx playwright test`（E2E全体）全件成功を確認

## 動作確認

- [ ] ローカル環境で`npx playwright test`が全自動実行できることを確認

## PR・レビュー

- [ ] PR を作成
- [ ] `/code-review` を実行し `code-review.md` を作成
- [ ] 指摘（🔴致命的は必須、🟡🔵はAI判断）に対応
