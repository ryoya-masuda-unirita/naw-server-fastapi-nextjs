# 詳細設計：ログインAPIのCORS未設定とセッションAPIのパス重複を修正

## 変更ファイル一覧

| ファイル | 種別 |
|---|---|
| `backend/app/core/config.py` | 変更（CORS設定フィールド追加） |
| `backend/app/main.py` | 変更（`CORSMiddleware` 追加） |
| `backend/.env` | 変更（CORS環境変数追加） |
| `backend/.env.example` | 変更（CORS環境変数追加） |
| `frontend/src/lib/constants/api-paths.ts` | 変更（`SESSION` パス修正） |

---

## `backend/app/core/config.py`

```python
from functools import lru_cache

from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    database_url: str
    secret_key: str
    cors_allowed_origins_raw: str = Field(default="", alias="CORS_ALLOWED_ORIGINS")
    cors_allowed_origin_regex: str | None = Field(
        default=None, alias="CORS_ALLOWED_ORIGIN_REGEX"
    )

    @property
    def cors_allowed_origins(self) -> list[str]:
        """CORS許可オリジンのリストを返す。

        pydantic-settings は list[str] 型のフィールドを環境変数から読む際に
        JSON 形式を要求するため、カンマ区切りの `.env` 値をそのまま扱えるよう
        raw な str フィールドを介してパースする。

        Returns:
            カンマ区切りをパースした許可オリジンのリスト。
        """
        return [
            origin.strip()
            for origin in self.cors_allowed_origins_raw.split(",")
            if origin.strip()
        ]


@lru_cache
def get_settings() -> Settings:
    return Settings()
```

`cors_allowed_origins_raw` フィールドは `alias="CORS_ALLOWED_ORIGINS"` により `.env` の `CORS_ALLOWED_ORIGINS` から読み込む（動作確認済み）。フィールド名をそのまま公開せず `cors_allowed_origins`（リスト）を `@property` で公開することで、呼び出し側（`main.py`）は素直に `list[str]` として扱える。

---

## `backend/app/main.py`

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.core.config import get_settings
from app.routers import health, auth
from app.routers.users import admin_router, user_router

app = FastAPI(title="naw-server FastAPI", version="0.1.0")

settings = get_settings()
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_allowed_origins,
    allow_origin_regex=settings.cors_allowed_origin_regex,
    allow_credentials=True,
    allow_methods=["GET", "POST", "PATCH", "DELETE"],
    allow_headers=["Authorization", "Content-Type"],
)

app.include_router(health.router)
app.include_router(auth.router)
app.include_router(auth.api_router)
app.include_router(admin_router)
app.include_router(user_router)
```

---

## `backend/.env` / `backend/.env.example`

```
DATABASE_URL=postgresql+asyncpg://root:root@localhost:5433/postgres
SECRET_KEY=changeme
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4201
CORS_ALLOWED_ORIGIN_REGEX=
```

`CORS_ALLOWED_ORIGIN_REGEX` は本 Issue 時点で対象ドメインが未確定のため空のままとする（`02_基本設計.md` 参照）。空文字列は `Settings.cors_allowed_origin_regex` で `None` として扱われるよう、`.env` 読み込み後に空文字を `None` に変換する処理が必要か確認する。

pydantic-settings は `str | None` フィールドに空文字 `""` が渡された場合、`None` ではなく空文字列 `""` のまま保持する。`CORSMiddleware(allow_origin_regex="")` は空文字列を正規表現として扱いマッチしなくなるだけで動作上の問題はないため、追加の変換処理は不要と判断する。

---

## `frontend/src/lib/constants/api-paths.ts`

```typescript
export const API_PATHS = {
  AUTH: {
    LOGIN: '/auth/login',
    LOGOUT: '/auth/logout',
    SESSION: '/auth',
    PASSWORD_RESET: '/auth/password/reset',
  },
} as const;
```

`SESSION` を `/api/auth` から `/auth` に変更する。`api-client.ts` の `API_BASE_URL`（`${VITE_API_URL}/api`）と結合すると `/api/auth` となり、バックエンドの実エンドポイント `GET /api/auth`（`backend/app/routers/auth.py` の `api_router`）と一致する。
