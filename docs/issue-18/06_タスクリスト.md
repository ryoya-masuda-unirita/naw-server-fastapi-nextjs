# タスクリスト：ログインAPIのCORS未設定とセッションAPIのパス重複を修正

## 完了条件

- [x] CORSMiddleware が組み込まれ、`http://localhost:5173`・`http://localhost:4201` からのリクエストが許可されること（`tests/unit/test_cors.py` で検証）
- [x] `frontend/src/lib/constants/api-paths.ts` の `SESSION` パス重複が解消されること
- [x] unit テストが全件パスすること
- [x] `/code-review` の 🔴 致命的指摘（3件）を全て修正すること（`code-review.md` 参照）
- [x] `frontend-angular` を使ったブラウザでの実ログインが成立すること（`08_動作確認.md` Step 1）
  - リロード後のセッション復元は、動作確認中に判明した別問題（Issue #20）によりブロックされているため、本Issueの完了条件からは除外し `08_動作確認.md`「未実施の確認事項」に記載した

---

## タスク

### 実装

- [x] 1. `backend/app/core/config.py` に CORS 関連フィールド（`cors_allowed_origins_raw`・`cors_allowed_origin_regex`・`cors_allowed_origins` プロパティ）を追加する
- [x] 2. `backend/app/main.py` に `CORSMiddleware` を追加する
- [x] 3. `backend/.env`・`backend/.env.example` に `CORS_ALLOWED_ORIGINS`・`CORS_ALLOWED_ORIGIN_REGEX` を追加する
- [x] 4. `frontend/src/lib/constants/api-paths.ts` の `API_PATHS.AUTH.SESSION` を `/auth` に修正する

### テスト

- [x] 5. `backend/tests/unit/test_config.py` を作成する
- [x] 6. `backend/tests/unit/test_cors.py` を作成する
- [x] 7. 全テストを実行しパスを確認する（`pytest`）

### PR・レビュー

- [x] 8. PR を作成する
- [x] 9. `/code-review` を実行し `code-review.md` を作成する
- [x] 10. 🔴 致命的指摘（3件）を修正する
- [x] 11. `08_動作確認.md` を完成させる

### code-review指摘対応（追加タスク）

- [x] 12. `backend/app/core/config.py` に `CorsSettings` を新設し、`Settings`（DB接続情報必須）から分離する
- [x] 13. `backend/app/main.py` の `allow_methods`/`allow_headers` を `["*"]` に変更する
- [x] 14. `CORS_ALLOWED_ORIGIN_REGEX` の空文字列を `None` に正規化するバリデータを追加する
- [x] 15. `backend/tests/unit/test_cors.py` を実アプリ（`app.main.app`）ベースのテストに書き換える
- [x] 16. `backend/tests/unit/test_config.py` に `import app.main` の回帰テストを追加する
- [x] 17. `frontend/src/lib/api-client.ts` の `API_BASE_URL` から自動 `/api` 付与を除去する（LOGIN/LOGOUT/PASSWORD_RESETのパス不整合の根本修正）
- [x] 18. `frontend/src/mocks/handlers/auth.ts` のモックのポート・パスを実際の構成に合わせる
- [x] 19. 全テスト（backend pytest・frontend vitest）が通ることを再確認する
