# 06_タスクリスト

## ドキュメント

- [x] `00_チケット内容.md` を作成
- [x] `01_要件定義.md`・`02_基本設計.md` を作成
- [x] `03_詳細設計.md`〜`05_テスト詳細設計.md` を作成

## 事前準備（#140相当のmypy設定を先行反映）

- [x] `backend/pyproject.toml` の `[tool.mypy]` に #140（PR #144）と同等の設定（`files`・`python_version`・`warn_unused_ignores`・`warn_redundant_casts`・除外`overrides`）を反映
- [x] `backend/tests/unit/test_azure_cost_client.py` の不要な `# type: ignore[arg-type]` を削除

## 実装

- [x] `app/services/index_service.py`: `additional_learning`の`room_id`に`assert room_id is not None`を追加
- [x] `app/services/message_service.py`: `content_response`に`MessageContentResponse | None`の型注釈を追加

## テスト

- [x] `uv run mypy app tests` を実行し、エラーが0件であることを確認
- [x] `uv run pytest tests/unit -q` を実行し、既存テストが全件成功することを確認
- [x] `uv run ruff check app tests` を実行し、Lintが通ることを確認

## 動作確認

- [x] `08_動作確認.md` に、自動テスト・mypy実行結果による確認内容を記録（本Issueはブラウザ操作を伴わない型修正のため、ブラウザでの動作確認は対象外）

## PR・レビュー

- [x] PR を作成（`Closes #141`）
- [x] `/code-review` を実行し `code-review.md` を作成
