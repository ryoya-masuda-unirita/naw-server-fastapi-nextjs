# DISABLED: モノレポ統合のため無効化。別リポジトリ用のデプロイ設定を参考として残す。
# ルートの .github/workflows/ に移す場合はパス・トリガーを見直すこと。
#
# name: Deploy to Dev (S3 + CloudFront)
#
# on:
#   push:
#     branches:
#       - develop
#   workflow_dispatch:
#
# env:
#   AWS_REGION: ap-northeast-1
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     environment: dev
#     permissions:
#       id-token: write
#       contents: read
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: npm
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Build Angular application
#         run: npm run build -- --configuration=dev
#
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}
#
#       - name: Deploy to S3
#         env:
#           S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
#         run: |
#           set -euo pipefail
#           DIST_DIR=dist/example/browser
#
#           aws s3 sync "$DIST_DIR/" "s3://${S3_BUCKET}/" \
#             --delete \
#             --cache-control "public, max-age=31536000"
#
#           aws s3 cp "$DIST_DIR/" "s3://${S3_BUCKET}/" \
#             --recursive \
#             --exclude "*" \
#             --include "*.html" \
#             --include "*.json" \
#             --cache-control "public, max-age=0, must-revalidate"
#
#       - name: Invalidate CloudFront cache
#         env:
#           CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }}
#         run: |
#           aws cloudfront create-invalidation \
#             --distribution-id "$CLOUDFRONT_DISTRIBUTION_ID" \
#             --paths "/*"
#
#       - name: Deployment summary
#         run: |
#           echo "Deployed to s3://${{ secrets.AWS_S3_BUCKET }}/"
#           echo "CloudFront distribution: ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }}"
